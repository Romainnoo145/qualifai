---
phase: 61.2-manual-prospect-parity
plan: '04'
subsystem: ui
tags:
  [typescript, react, tailwind, tdd, testing-library, null-guards, amber-pill]

dependency_graph:
  requires:
    - phase: 61.2-03
      provides: createAndProcess with 6 optional enrichment fields, Optionele verrijking form
    - phase: 61.1
      provides: ProspectLogo, ProspectLastRunStatus amber pattern (bg-amber-50 text-amber-800 border-amber-300)
  provides:
    - components/features/prospects/prospect-enrichment-badge.tsx::ProspectEnrichmentBadge
    - ProspectEnrichmentBadge mounted in app/admin/prospects/[id]/page.tsx detail header
    - Null guard audit on detail page (all Apollo-optional fields confirmed safe)
  affects:
    - Phase 62 (any component rendering prospect.companyName/industry/description in new pages)

tech-stack:
  added: []
  patterns:
    - Amber pill pattern reused from Phase 61.1 ProspectLastRunStatus (bg-amber-50 text-amber-800 border-amber-300)
    - FIELD_LABELS record maps field keys to Dutch display names — single source of truth for tooltip copy
    - Component returns null when all fields populated (no empty DOM node)
    - title= attribute for tooltip (no third-party tooltip library)
    - RTL tests use getByText / queryByText / getByTitle for amber pill assertions

key-files:
  created:
    - components/features/prospects/prospect-enrichment-badge.tsx
    - components/features/prospects/prospect-enrichment-badge.test.tsx
  modified:
    - app/admin/prospects/[id]/page.tsx

key-decisions:
  - 'Amber pill uses title= attribute for tooltip — no third-party tooltip library, simpler, consistent with existing codebase patterns'
  - 'Null guard audit found no unguarded accesses — existing {p.industry && ...} conditional rendering guards were already sufficient across the detail page'
  - 'CompanyVitals and CommandCenter confirmed as dead code (imported but never mounted as JSX) — dead imports removed from detail page'

patterns-established:
  - 'ProspectEnrichmentBadge pattern: component returns null when all props populated, renders amber pill when any are missing — reusable for other optional-field indicators'
  - 'FIELD_LABELS record pattern: map field key to Dutch display name at module scope, join missing field labels into tooltip string — avoids duplicating translation logic in tests and component'

requirements-completed:
  - PARITY-11
  - PARITY-12
  - PARITY-13
  - PARITY-14
  - PARITY-15

duration: ~35min
completed: '2026-04-14'
---

# Phase 61.2 Plan 04: Render Parity Audit + ProspectEnrichmentBadge Summary

**`ProspectEnrichmentBadge` amber pill with Dutch tooltip mounted in detail header; null guard audit confirmed existing guards sufficient; Evidence + Analysis tabs render cleanly for manual prospects — all 5 human-verify steps passed by Romano.**

## Performance

- **Duration:** ~35 min
- **Completed:** 2026-04-14
- **Tasks:** 3/3 (Task 1 auto, Task 2 auto, Task 3 human-verify: approved)
- **Files modified:** 3

## Accomplishments

- New `ProspectEnrichmentBadge` component renders amber pill "Verrijking onvolledig" with Dutch tooltip listing missing fields (bedrijfsnaam / sector / omschrijving) — returns null when all three populated
- Badge mounted in detail page hero next to company name; receives `prospect.companyName`, `prospect.industry`, `prospect.description` as props
- Null guard audit on `app/admin/prospects/[id]/page.tsx` confirmed all Apollo-optional fields already guarded with `{p.field && ...}` conditionals; dead imports (CompanyVitals, CommandCenter) removed
- 5-case unit test suite covering all render/hide scenarios and tooltip content
- Romano completed 5-step manual verification — all steps passed, including logo quality improvement for Marfa

## Task Commits

1. **Task 1: ProspectEnrichmentBadge component + tests (TDD)** — `6cfd1b2` (feat)
2. **Task 2: Mount badge in detail page + null guard audit** — `0bbb346` (feat)
3. **Task 3: Human-verify checkpoint** — approved by Romano (all 5 steps passed)

## Files Created/Modified

- `components/features/prospects/prospect-enrichment-badge.tsx` — Amber pill component; renders when any of companyName/industry/description is null/undefined; Dutch tooltip via `title=` attribute; returns null when all three populated
- `components/features/prospects/prospect-enrichment-badge.test.tsx` — 5 RTL tests: pill renders on all-null, pill renders on partial null, no pill when all populated, tooltip contains correct Dutch field names for partial missing, tooltip contains all three field names when all missing
- `app/admin/prospects/[id]/page.tsx` — ProspectEnrichmentBadge imported and mounted in detail header hero section; dead imports (CompanyVitals, CommandCenter) removed

## Decisions Made

- Amber pill uses `title=` attribute for tooltip — no third-party tooltip library needed; consistent with existing admin UI patterns and simpler to test with `getByTitle`
- Null guard audit found no deficiencies — the RESEARCH.md pre-analysis was correct: all Apollo-optional fields in the detail page are wrapped in `{p.field && ...}` conditional rendering guards already. No fixes were required.
- Dead imports (CompanyVitals, CommandCenter) were removed from the import block — both components exist on disk but are not mounted anywhere in the detail page. Removed the import lines only; source files left in place.

## Manual Verification Result

Romano walked through all 5 steps on 2026-04-14 with the Marfa prospect and confirmed:

| Step | Description                                                                                | Result |
| ---- | ------------------------------------------------------------------------------------------ | ------ |
| 1    | Apollo no-coverage amber toast on "Verrijk opnieuw" — verbatim Dutch message, no red error | PASSED |
| 2    | ProspectEnrichmentBadge amber pill visible in detail header with correct tooltip           | PASSED |
| 3    | Evidence tab and Analysis tab render cleanly without crashes                               | PASSED |
| 4    | /admin/prospects/new Optionele verrijking collapsible works with 6 fields                  | PASSED |
| 5    | Logo quality improved — Marfa now shows real high-res logo (not 16x16 favicon globe)       | PASSED |

All 4 Phase 61.2 deliverables confirmed end-to-end:

1. Apollo 422 → amber toast (Plan 01)
2. og:image scraper for higher-resolution logos (Plan 02)
3. "Optionele verrijking" collapsible on /admin/prospects/new (Plan 03)
4. ProspectEnrichmentBadge amber pill + null guard audit (Plan 04)

## Deviations from Plan

None — plan executed exactly as written. The null guard audit finding (no fixes needed) was anticipated by RESEARCH.md and confirmed by the audit.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

Phase 61.2 is fully complete. All 15 PARITY requirements satisfied across 4 plans:

- PARITY-01 through PARITY-05 (Plan 01: Apollo no-coverage error handling)
- PARITY-06 (Plan 02: og-logo helper)
- PARITY-07 through PARITY-10 (Plan 03: og-logo wiring + form)
- PARITY-11 through PARITY-15 (Plan 04: render parity + badge)

Manual prospects created via "Optionele verrijking" form are now fully usable: logos render at real quality, enrichment errors surface as amber toasts, detail cards have content, and evidence/analysis pages render cleanly.

Ready for Phase 62: Client-Facing Voorstel + PDF Worker.

---

_Phase: 61.2-manual-prospect-parity_
_Completed: 2026-04-14_
