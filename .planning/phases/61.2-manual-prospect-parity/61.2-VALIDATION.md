---
phase: 61.2
slug: manual-prospect-parity
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-14
---

# Phase 61.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property               | Value                                      |
| ---------------------- | ------------------------------------------ |
| **Framework**          | vitest 4.x                                 |
| **Config file**        | `vitest.config.ts`                         |
| **Quick run command**  | `npx vitest run <test-file>`               |
| **Full suite command** | `npm run test`                             |
| **Estimated runtime**  | ~30s for full suite, ~1s per targeted file |

---

## Sampling Rate

- **After every task commit:** Run the targeted `npx vitest run <test-file>` for the file under test
- **After every plan wave:** Run `npm run check` (typecheck + lint) plus the related vitest files
- **Before human-verify checkpoint (Plan 04):** Full `npm run test` must be green
- **Max feedback latency:** ~5 seconds for targeted runs

---

## Per-Task Verification Map

| Task ID    | Plan | Wave | Requirement                     | Test Type          | Automated Command                                                                                                                                                                     | File Exists | Status     |
| ---------- | ---- | ---- | ------------------------------- | ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ---------- |
| 61.2-01-01 | 01   | 1    | PARITY-01, PARITY-02            | unit               | `npx vitest run lib/enrichment/service.test.ts`                                                                                                                                       | ❌ W0       | ⬜ pending |
| 61.2-01-02 | 01   | 1    | PARITY-03, PARITY-04, PARITY-05 | unit + grep        | `npx vitest run server/routers/admin.enrichProspect.test.ts` + `grep -c "FRIENDLY_ERROR_APOLLO_NO_COVERAGE" components/features/prospects/error-mapping.ts`                           | ❌ W0       | ⬜ pending |
| 61.2-02-01 | 02   | 1    | PARITY-06                       | unit               | `npx vitest run lib/enrichment/og-logo.test.ts`                                                                                                                                       | ❌ W0       | ⬜ pending |
| 61.2-02-02 | 02   | 1    | PARITY-08 (test coverage)       | unit               | `npx vitest run lib/enrichment/og-logo.test.ts -- --coverage`                                                                                                                         | ❌ W0       | ⬜ pending |
| 61.2-03-01 | 03   | 2    | PARITY-07                       | grep + integration | `grep -c "getHighResLogoUrl(" server/routers/admin.ts` ≥ 2 + `npx vitest run server/routers/admin.createProspect.test.ts`                                                             | ❌ W0       | ⬜ pending |
| 61.2-03-02 | 03   | 2    | PARITY-09, PARITY-10            | unit + integration | `npx vitest run server/routers/admin.createAndProcess.test.ts` + grep for new Zod fields                                                                                              | ❌ W0       | ⬜ pending |
| 61.2-03-03 | 03   | 2    | PARITY-11                       | grep               | `grep -c "Optionele verrijking" app/admin/prospects/new/page.tsx` = 1                                                                                                                 | ⬜ N/A      | ⬜ pending |
| 61.2-04-01 | 04   | 3    | PARITY-12, PARITY-13            | unit + grep        | `npx vitest run components/features/prospects/prospect-enrichment-badge.test.tsx` + `grep -c "Verrijking onvolledig" components/features/prospects/prospect-enrichment-badge.tsx` = 1 | ❌ W0       | ⬜ pending |
| 61.2-04-02 | 04   | 3    | PARITY-14                       | grep + audit       | `grep -c "ProspectEnrichmentBadge" app/admin/prospects/\[id\]/page.tsx` = 1 + manual null-guard audit walkthrough                                                                     | ⬜ N/A      | ⬜ pending |
| 61.2-04-03 | 04   | 3    | PARITY-15                       | human-verify       | Romano walks Marfa through detail + Evidence + Analysis pages, confirms zero crashes + amber pill renders                                                                             | ⬜ N/A      | ⬜ pending |

_Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky_

_"❌ W0" = test file does not exist yet; will be created in the same task as the implementation (TDD). This is acceptable because each task creates BOTH the implementation and its test in a single commit, and the executor runs the test after creation as part of the task's `<verify>` block._

---

## Wave 0 Requirements

No standalone Wave 0 needed. Each plan creates its own test file as part of the implementation task (TDD pattern matching Phase 61.1 conventions). Specifically:

- 61.2-01 Task 1 creates `lib/enrichment/service.test.ts` test cases for `EnrichmentNoCoverageError` path alongside the implementation
- 61.2-01 Task 2 creates `server/routers/admin.enrichProspect.test.ts` (or extends if exists) for the partial-success return shape
- 61.2-02 Task 2 creates `lib/enrichment/og-logo.test.ts` with 6+ cases (og:image found, twitter fallback, apple-touch fallback, link-icon fallback, all-fail, relative URL resolve, HEAD probe failure, timeout)
- 61.2-03 Task 1 extends/creates `server/routers/admin.createProspect.test.ts` covering the og-logo IIFE replacement
- 61.2-03 Task 2 extends/creates `server/routers/admin.createAndProcess.test.ts` covering Zod schema additions and the sticky-against-Apollo guard
- 61.2-04 Task 1 creates `components/features/prospects/prospect-enrichment-badge.test.tsx` covering the four visual states (complete / missing companyName / missing industry / missing description)

---

## Manual-Only Verifications

| Behavior                                                                       | Requirement          | Why Manual                                                                                                                  | Test Instructions                                                                                                                                                                                                            |
| ------------------------------------------------------------------------------ | -------------------- | --------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Marfa logo actually renders at high resolution after og-logo wiring            | PARITY-06, PARITY-07 | Real network behavior on Wix sites cannot be unit-mocked. Wix may serve different og:image content depending on user agent. | After Plan 03 ships, run `tsx scripts/tmp-rerun-marfa.ts` (or trigger via Acties panel "Verrijk opnieuw"), then visit /admin/prospects/<marfa-id> and confirm a high-res logo replaces the favicon globe.                    |
| ProspectEnrichmentBadge amber pill renders with correct Dutch tooltip on Marfa | PARITY-12, PARITY-13 | Visual + tooltip behavior — only meaningful when actually rendered in the browser.                                          | Plan 04 human-verify checkpoint: visit Marfa detail page, hover the pill, screenshot tooltip text matching exactly: "Verrijking onvolledig — companyName / industry / description ontbreken"                                 |
| Detail / Evidence / Analysis pages do not crash on any null Apollo field       | PARITY-14, PARITY-15 | Click-through audit cannot be fully scripted.                                                                               | Plan 04 human-verify checkpoint: walk Marfa through detail → evidence tab → analysis tab → contacts tab. Open browser devtools console. Confirm zero red errors. Screenshot if any field renders "n/a" gracefully.           |
| Apollo no-coverage amber toast surfaces with verbatim Dutch text               | PARITY-04, PARITY-05 | Only meaningful in real browser interaction with the Acties panel.                                                          | After Plan 01 ships, click "Verrijk opnieuw" on Marfa in /admin/prospects/<marfa-id>. Confirm amber (not red) toast appears with Dutch text "Apollo heeft deze organisatie niet in zijn database — verrijking gedeeltelijk." |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify commands or are explicitly scoped human-verify
- [x] Sampling continuity: every code task has automated verification in the same commit as implementation
- [x] Wave 0 not needed — TDD inline pattern matches Phase 61.1 convention
- [x] No watch-mode flags — all commands use `vitest run` (single execution)
- [x] Feedback latency < 5s for targeted runs
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-04-14
