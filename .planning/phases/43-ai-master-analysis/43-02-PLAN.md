---
phase: 43-ai-master-analysis
plan: 02
type: execute
wave: 2
depends_on: [43-01]
files_modified:
  - lib/research-executor.ts
  - server/routers/research.ts
autonomous: true
requirements: [ANLS-01, ANLS-06]

must_haves:
  truths:
    - 'Running research on an Atlantis prospect triggers master analysis after intent extraction and RAG retrieval'
    - 'Analysis output is persisted to ProspectAnalysis table and loadable without AI calls'
    - 'Analysis failure does not block research pipeline completion'
    - 'Admin can inspect analysis output via tRPC endpoint'
  artifacts:
    - path: 'lib/research-executor.ts'
      provides: 'Master analysis step integrated into ATLANTIS research pipeline'
      contains: 'generateMasterAnalysis'
    - path: 'server/routers/research.ts'
      provides: 'tRPC endpoint to fetch ProspectAnalysis for a prospect'
      exports: ['getProspectAnalysis']
  key_links:
    - from: 'lib/research-executor.ts'
      to: 'lib/analysis/master-analyzer.ts'
      via: 'generateMasterAnalysis call after RAG retrieval'
      pattern: 'generateMasterAnalysis'
    - from: 'lib/research-executor.ts'
      to: 'prisma.prospectAnalysis.create'
      via: 'DB persistence of analysis result'
      pattern: "prospectAnalysis\\.create"
    - from: 'server/routers/research.ts'
      to: 'prisma.prospectAnalysis.findFirst'
      via: 'tRPC query for admin inspection'
      pattern: "prospectAnalysis\\.findFirst"
---

<objective>
Integrate master analysis into the Atlantis research pipeline and add persistence + retrieval endpoints so analysis content survives without AI calls.

Purpose: Connect the analysis engine (Plan 01) to the live pipeline so running research produces persisted discover content. This is the critical wiring that makes analysis output available for Phase 44 rendering.
Output: Research executor calls generateMasterAnalysis, persists result to ProspectAnalysis, tRPC endpoint exposes it.
</objective>

<execution_context>
@/home/klarifai/.claude/get-shit-done/workflows/execute-plan.md
@/home/klarifai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-ai-master-analysis/43-CONTEXT.md
@.planning/phases/43-ai-master-analysis/43-01-SUMMARY.md

Key source files:
@lib/research-executor.ts (pipeline to integrate into — lines ~1320-1690 are the ATLANTIS block)
@lib/analysis/master-analyzer.ts (from Plan 01 — generateMasterAnalysis function)
@lib/analysis/types.ts (from Plan 01 — MasterAnalysis, MasterAnalysisInput types)
@server/routers/research.ts (add getProspectAnalysis endpoint)
@lib/rag/retriever.ts (RagRetrievedPassage type — already available in pipeline scope)
@lib/extraction/types.ts (IntentVariables — already available in pipeline scope)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate master analysis into research pipeline with DB persistence</name>
  <files>
    lib/research-executor.ts
  </files>
  <action>
1. In research-executor.ts, import generateMasterAnalysis from `@/lib/analysis/master-analyzer` and MasterAnalysisInput from `@/lib/analysis/types`.

2. After the existing ATLANTIS RAG retrieval block (around line 1530 where ragPassages are populated) and BEFORE the opportunity/trigger generation section (around line 1600), add the master analysis step:
   - Only runs when `prospect.project.projectType === 'ATLANTIS'`
   - Build MasterAnalysisInput from values already in scope:
     - intentVars: the `intentVars` variable (from extraction step ~line 1319)
     - passages: the `ragPassages` variable (from RAG retrieval)
     - prospect: map from `prospect` record to AnalysisProspectProfile shape
     - spvs: query active SPVs for the project: `db.sPV.findMany({ where: { projectId: prospect.projectId, isActive: true } })`
   - Call `generateMasterAnalysis(input)` wrapped in try/catch
   - On success: persist via `db.prospectAnalysis.create({ data: { researchRunId: run.id, prospectId: input.prospectId, version: 'analysis-v1', content: toJson(result), modelUsed: result.modelUsed, inputSnapshot: toJson({ intentVarCount: intentVars?.populatedCount ?? 0, passageCount: ragPassages.length, spvCount: spvs.length }) } })`
   - Add diagnostic: `{ source: 'master_analysis', status: 'ok', message: 'Master analysis generated: ${result.triggers.length} triggers, ${result.tracks.length} tracks' }`
   - On failure: add diagnostic with status 'warning', do NOT throw — pipeline continues with legacy template generators as fallback
   - Add 'master_analysis' to the SourceDiagnostic source union type

3. The existing opportunity-generator and trigger-generator calls for ATLANTIS remain as fallback — they still run. Phase 44 will read from ProspectAnalysis when available, falling back to the old snapshot data when not.

  </action>
  <verify>
    <automated>cd /home/klarifai/Documents/klarifai/projects/qualifai && npx tsc --noEmit 2>&1 | grep -v sitemap.test | head -20</automated>
  </verify>
  <done>Research pipeline for ATLANTIS prospects calls generateMasterAnalysis after RAG retrieval, persists MasterAnalysis to ProspectAnalysis table, adds master_analysis diagnostic, and gracefully degrades on failure</done>
</task>

<task type="auto">
  <name>Task 2: tRPC endpoint for analysis retrieval</name>
  <files>
    server/routers/research.ts
  </files>
  <action>
1. Add `getProspectAnalysis` query to the research router in `server/routers/research.ts`:
   - Input: `{ prospectId: z.string() }`
   - Query: `db.prospectAnalysis.findFirst({ where: { prospectId: input.prospectId }, orderBy: { createdAt: 'desc' } })`
   - Returns the most recent ProspectAnalysis for the prospect (or null)
   - Protected by existing auth middleware (same as other research router queries)

2. This endpoint serves two consumers:
   - Admin prospect detail page (Phase 43 — inspection of generated content)
   - Discover page server component (Phase 44 — rendering without AI calls, satisfying ANLS-06)

  </action>
  <verify>
    <automated>cd /home/klarifai/Documents/klarifai/projects/qualifai && npx tsc --noEmit 2>&1 | grep -v sitemap.test | head -20</automated>
  </verify>
  <done>getProspectAnalysis tRPC query returns the most recent MasterAnalysis content for a prospect, loadable without any AI calls — satisfying ANLS-06 persistence requirement</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (excluding pre-existing sitemap.test.ts)
- research-executor.ts imports and calls generateMasterAnalysis for ATLANTIS prospects
- ProspectAnalysis is created in DB after successful analysis generation
- master_analysis diagnostic appears in pipeline output
- getProspectAnalysis tRPC endpoint is callable and returns analysis content
- Analysis failure produces warning diagnostic but does not block pipeline completion
</verification>

<success_criteria>
Running research on an Atlantis prospect produces a ProspectAnalysis record containing AI-generated context, triggers, and partnership tracks. The content is retrievable via tRPC without further AI calls. The pipeline does not break if analysis generation fails.
</success_criteria>

<output>
After completion, create `.planning/phases/43-ai-master-analysis/43-02-SUMMARY.md`
</output>
