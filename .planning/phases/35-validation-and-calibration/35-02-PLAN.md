---
phase: 35-validation-and-calibration
plan: '02'
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-discover-validation.mjs
autonomous: false
requirements:
  - VALID-01

must_haves:
  truths:
    - 'At least one hypothesis is confirmed (ACCEPTED) via /discover/ browser interaction'
    - 'At least one hypothesis is declined (DECLINED) via /discover/ browser interaction'
    - 'DB assertion script confirms ACCEPTED and DECLINED status transitions persisted'
    - 'Admin detail view shows updated hypothesis statuses after validation'
  artifacts:
    - path: 'scripts/check-discover-validation.mjs'
      provides: 'DB assertion script for /discover/ validation results'
      contains: 'workflowHypothesis'
  key_links:
    - from: 'scripts/check-discover-validation.mjs'
      to: 'WorkflowHypothesis table'
      via: 'Prisma query checking status transitions'
      pattern: 'status.*ACCEPTED|DECLINED'
---

<objective>
Run a live /discover/ validation session with a real prospect to verify the hypothesis confirm/decline flow works end-to-end, with automated DB assertions confirming the state transitions persisted.

Purpose: VALID-01 confirms the /discover/ hypothesis validation flow — built in Phase 19 — actually works when a real prospect interacts with it. This is the first real browser test of the confirm/decline mechanism.
Output: DB assertion script, validated hypothesis status transitions, and visual confirmation in admin UI.
</objective>

<execution_context>
@/home/klarifai/.claude/get-shit-done/workflows/execute-plan.md
@/home/klarifai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-validation-and-calibration/35-RESEARCH.md
@.planning/phases/19-client-hypothesis-validation/19-01-SUMMARY.md
@server/routers/hypotheses.ts
@server/trpc.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DB assertion script for /discover/ validation</name>
  <files>
    scripts/check-discover-validation.mjs
  </files>
  <action>
Create `scripts/check-discover-validation.mjs` that verifies hypothesis validation state after a browser session. Follow the established project script pattern (see scripts/check-hypotheses.mjs).

The script must:

1. Use `import 'dotenv/config'`, pg Pool, PrismaPg adapter, PrismaClient
2. Query WorkflowHypothesis for Mujjo (slug: `3Bi1vv2M`) — this is the only prospect with status allowing /discover/ access (ENGAGED)
3. Output a table of all hypotheses with: id (truncated to 8 chars), title, status, confidenceScore
4. Run automated assertions:
   - Assert at least one hypothesis has status = 'ACCEPTED' (confirmed by prospect)
   - Assert at least one hypothesis has status = 'DECLINED' (declined by prospect)
   - Print PASS/FAIL for each assertion with descriptive messages
5. Exit with code 0 if all assertions pass, code 1 if any fail

The script is designed to be run AFTER the human browser session on `/discover/mujjo-3Bi1vv2M`. Before the session, all hypotheses for Mujjo will have DRAFT status (or mixed DRAFT/ACCEPTED from earlier runs). After the session, at least one should be ACCEPTED and one DECLINED.

**Important:** The `validateByProspect` mutation uses prospectProcedure which matches on the prospect's `slug` field (the 8-char nanoid `3Bi1vv2M`). The /discover/ URL format is `/discover/{readableSlug}-{slug}` = `/discover/mujjo-3Bi1vv2M`.

**Note on prospect lookup:** Query by prospect slug `3Bi1vv2M`:

```javascript
const prospect = await db.prospect.findFirst({ where: { slug: '3Bi1vv2M' } });
const hyps = await db.workflowHypothesis.findMany({
  where: { prospectId: prospect.id },
  select: { id: true, title: true, status: true, confidenceScore: true },
  orderBy: { confidenceScore: 'desc' },
});
```

  </action>
  <verify>
    <automated>cd /home/klarifai/Documents/klarifai/projects/qualifai && node -e "import('dotenv/config').then(() => console.log('dotenv OK'))" && test -f scripts/check-discover-validation.mjs && echo "PASS" || echo "FAIL"</automated>
    <manual>Review script logic matches the expected assertion pattern</manual>
  </verify>
  <done>
    check-discover-validation.mjs exists, imports correctly, and is ready to run after a browser session. Script queries Mujjo hypotheses and asserts ACCEPTED + DECLINED statuses.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run /discover/ validation session in browser</name>
  <files>scripts/check-discover-validation.mjs</files>
  <action>
CHECKPOINT: Human browser session required. The /discover/ hypothesis validation flow (built in Phase 19) with confirm/decline buttons on the prospect dashboard. The assertion script (Task 1) is ready to verify results after the browser session.

**Prerequisites:**

- Dev server running on port 9200 (`npm run dev`)
- Database accessible (qualifai-db on port 5433)

**Steps for user:**

1. Open browser to `http://localhost:9200/discover/mujjo-3Bi1vv2M`
2. You should see the Mujjo prospect dashboard with hypothesis cards
3. **Confirm** one hypothesis — click the confirm/accept button on a hypothesis you agree with
4. **Decline** one hypothesis — click the decline button on a different hypothesis
5. Verify the UI updates to reflect the new statuses (confirmed shows green, declined shows visual indicator)

**After browser session, run assertions:**

```bash
node scripts/check-discover-validation.mjs
```

Expected output: table of hypotheses showing at least one ACCEPTED and one DECLINED, with PASS assertions.

**Then check admin view:** 6. Open `http://localhost:9200/admin/prospects/{mujjo-id}` (find the Mujjo prospect in admin list) 7. Check the Analysis tab — hypothesis cards should show updated status chips ("Confirmed by prospect" in green for ACCEPTED, visual indicator for DECLINED)

**Resume signal:** Type "approved" if both confirm and decline worked and DB assertions passed, or describe any issues encountered.
</action>
<verify>
<automated>cd /home/klarifai/Documents/klarifai/projects/qualifai && node scripts/check-discover-validation.mjs</automated>
<manual>Browser session: /discover/ page loads, confirm works, decline works. Admin view shows updated statuses.</manual>
</verify>
<done>
At least one hypothesis ACCEPTED and one DECLINED in DB. Assertion script passes. Admin detail view shows updated status chips.
</done>
</task>

</tasks>

<verification>
1. `test -f scripts/check-discover-validation.mjs` — assertion script exists
2. `node scripts/check-discover-validation.mjs` — returns exit code 0 with PASS for both assertions (after browser session)
3. Human confirmed: /discover/ page loads, confirm works, decline works
4. Human confirmed: admin detail view shows updated hypothesis statuses
</verification>

<success_criteria>

- /discover/ validation session completed with at least one real prospect (Mujjo)
- Both confirm and decline interactions recorded in DB (WorkflowHypothesis status = ACCEPTED and DECLINED)
- Automated DB assertion script confirms state transitions
- Admin detail view shows updated hypothesis statuses
  </success_criteria>

<output>
After completion, create `.planning/phases/35-validation-and-calibration/35-02-SUMMARY.md`
</output>
