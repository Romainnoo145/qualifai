---
phase: 35-validation-and-calibration
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/enrichment/crawl4ai.ts
  - scripts/calibration-report.mjs
  - .planning/phases/35-validation-and-calibration/35-calibration-report.md
autonomous: true
requirements:
  - VALID-02
  - VALID-03

must_haves:
  truths:
    - 'crawl4ai.ts passes remove_overlay_elements and flatten_shadow_dom to CrawlerRunConfig'
    - 'Calibration report script applies aiRelevance >= 0.5 filter and uses MIN_AVERAGE_CONFIDENCE = 0.55 threshold'
    - 'Calibration report runs against all 7+ prospects and outputs correct gate tier for each'
    - 'Calibration report markdown documents before/after distribution with threshold recommendation'
    - 'quality-config.ts thresholds are confirmed correct (or updated) with inline calibration comment'
  artifacts:
    - path: 'lib/enrichment/crawl4ai.ts'
      provides: 'Crawl4AI v0.8.x params in request body'
      contains: 'remove_overlay_elements'
    - path: 'scripts/calibration-report.mjs'
      provides: 'Correct calibration script mirroring quality-config.ts logic'
      contains: 'AI_RELEVANCE_THRESHOLD'
    - path: '.planning/phases/35-validation-and-calibration/35-calibration-report.md'
      provides: 'Documented calibration results with threshold analysis'
      contains: 'MIN_AVERAGE_CONFIDENCE'
  key_links:
    - from: 'scripts/calibration-report.mjs'
      to: 'lib/quality-config.ts'
      via: 'threshold constants mirrored'
      pattern: "MIN_AVERAGE_CONFIDENCE.*0\\.55"
---

<objective>
Update Crawl4AI params for v0.8.x features and create a correct calibration report script that mirrors the actual quality gate logic, then run it against all real prospects to document the evidence distribution and confirm threshold constants.

Purpose: VALID-02 ensures the codebase is ready for Crawl4AI v0.8.x consent popup removal and shadow DOM flattening. VALID-03 confirms pain gate thresholds are correctly tuned to real prospect evidence distributions.
Output: Updated crawl4ai.ts, new calibration-report.mjs script, and a calibration report markdown documenting the analysis.
</objective>

<execution_context>
@/home/klarifai/.claude/get-shit-done/workflows/execute-plan.md
@/home/klarifai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-validation-and-calibration/35-RESEARCH.md
@lib/enrichment/crawl4ai.ts
@lib/quality-config.ts
@scripts/calibration-table.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Crawl4AI v0.8.x params and create calibration report script</name>
  <files>
    lib/enrichment/crawl4ai.ts
    scripts/calibration-report.mjs
  </files>
  <action>
**crawl4ai.ts update:**
Add two new params to the `crawler_config.params` object in the `extractMarkdown` function:
- `remove_overlay_elements: true` — v0.8.x consent popup removal
- `flatten_shadow_dom: true` — v0.8.x shadow DOM flattening

Place them after `simulate_user: true` and before `wait_for_timeout: 15000`. Add inline comments:

```
remove_overlay_elements: true,   // v0.8.x: remove consent popups/overlays
flatten_shadow_dom: true,        // v0.8.x: flatten shadow DOM for Web Components
```

These params are backward-compatible — older Crawl4AI versions ignore unknown params.

**calibration-report.mjs creation:**
Create a new script `scripts/calibration-report.mjs` (NOT modifying the old calibration-table.mjs — that file is an untracked historical artifact). The new script must:

1. Use the established script pattern: `import 'dotenv/config'`, pg Pool, PrismaPg adapter, PrismaClient
2. Mirror the EXACT quality gate logic from quality-config.ts:
   - `MIN_EVIDENCE_COUNT = 3`
   - `GREEN_MIN_SOURCE_TYPES = 3`
   - `MIN_AVERAGE_CONFIDENCE = 0.55`
   - `AI_RELEVANCE_THRESHOLD = 0.5` (items with aiRelevance below this are excluded from confidence average — this matches workflow-engine.ts evaluateQualityGate)
3. For each prospect, fetch the latest research run with evidenceItems (including metadata for aiRelevance)
4. Compute: total evidence count, distinct source types, raw average confidence, scorable count (items passing aiRelevance filter), scorable average confidence, gate tier (RED/AMBER/GREEN)
5. Output a formatted table to stdout with all metrics
6. After the table, output a threshold analysis summary:
   - Current threshold: MIN_AVERAGE_CONFIDENCE = 0.55
   - Range of scorable averages across all prospects
   - Margin (lowest scorable avg - threshold)
   - Recommendation: "No change needed" or "Adjust to X.XX"

See the code example in 35-RESEARCH.md for the correct Prisma query pattern with aiRelevance filter logic.
</action>
<verify>
<automated>cd /home/klarifai/Documents/klarifai/projects/qualifai && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
<manual>Run `node scripts/calibration-report.mjs` and verify all 7 prospects show GREEN tier</manual>
</verify>
<done>
crawl4ai.ts includes remove_overlay_elements and flatten_shadow_dom params. calibration-report.mjs runs successfully against DB, applies aiRelevance filter, uses 0.55 threshold, and outputs correct gate tiers for all prospects.
</done>
</task>

<task type="auto">
  <name>Task 2: Run calibration and document results</name>
  <files>
    .planning/phases/35-validation-and-calibration/35-calibration-report.md
    lib/quality-config.ts
  </files>
  <action>
**Run the calibration script:**
Execute `node scripts/calibration-report.mjs` and capture the full output.

**Create calibration report:**
Write `.planning/phases/35-validation-and-calibration/35-calibration-report.md` documenting:

1. **Methodology:** How the calibration was performed (aiRelevance filter, scorable average, source type count)
2. **Results table:** Full output from calibration-report.mjs — all 7+ prospects with their metrics and tiers
3. **Distribution analysis:**
   - Scorable average confidence range (min to max)
   - Scorable count range (how many items per prospect pass the aiRelevance filter)
   - Source type distribution
4. **Threshold evaluation:**
   - Current: MIN_AVERAGE_CONFIDENCE = 0.55
   - Lowest scorable avg in dataset (expected: Marcore at ~0.59)
   - Margin above threshold (expected: ~0.04)
   - Current: GREEN_MIN_SOURCE_TYPES = 3
   - All prospects have 4-5 source types — well above threshold
5. **Recommendation:** Based on the research data showing all 7 prospects at 0.59-0.70 scorable avg, the 0.55 threshold is correct. Document this as "thresholds confirmed correct — no adjustment needed"
6. **Before/after comparison:** The old calibration-table.mjs used 0.65 threshold + no aiRelevance filter (showed all AMBER). The new script uses 0.55 + aiRelevance filter (shows all GREEN). Explain why the difference matters.

**Update quality-config.ts inline comment:**
Add a calibration date comment to the `MIN_AVERAGE_CONFIDENCE` constant:

```
/** Minimum average confidence score (secondary signal — computed over items with aiRelevance >= 0.50 only)
 * Calibrated 2026-03-02: 7 prospects, scorable avg range 0.59-0.70, margin +0.04 above threshold */
```

If the calibration data shows thresholds need adjustment (unexpected — research says they don't), update the constant value AND document the reasoning.

**Crawl4AI documentation:**
Add a brief section to the calibration report noting: "Crawl4AI v0.8.x params (`remove_overlay_elements`, `flatten_shadow_dom`) added to crawl4ai.ts. Live verification pending Crawl4AI service startup — params are backward-compatible with v0.7.x."

If Crawl4AI service is available (check `curl -s http://localhost:11235/health`), run a quick test against one real URL and document the result. If not available, document as infrastructure gap — not a code bug.
</action>
<verify>
<automated>cd /home/klarifai/Documents/klarifai/projects/qualifai && test -f .planning/phases/35-validation-and-calibration/35-calibration-report.md && grep -q "MIN_AVERAGE_CONFIDENCE" .planning/phases/35-validation-and-calibration/35-calibration-report.md && grep -q "Calibrated 2026" lib/quality-config.ts && echo "PASS" || echo "FAIL"</automated>
<manual>Review calibration report for completeness and threshold confirmation</manual>
</verify>
<done>
Calibration report documents full distribution analysis for all 7+ prospects with correct gate logic. quality-config.ts has updated calibration comment. Threshold is either confirmed correct at 0.55 or adjusted with documented reasoning. Crawl4AI v0.8.x status is documented.
</done>
</task>

</tasks>

<verification>
1. `grep -c 'remove_overlay_elements' lib/enrichment/crawl4ai.ts` returns 1
2. `grep -c 'flatten_shadow_dom' lib/enrichment/crawl4ai.ts` returns 1
3. `node scripts/calibration-report.mjs` runs without errors and shows all prospects with tier assignment
4. `cat .planning/phases/35-validation-and-calibration/35-calibration-report.md` contains distribution analysis and threshold recommendation
5. `grep 'Calibrated 2026' lib/quality-config.ts` returns the updated comment
6. `npx tsc --noEmit` passes with zero errors
</verification>

<success_criteria>

- Crawl4AI v0.8.x params added to crawl4ai.ts (backward-compatible, ready for service upgrade)
- Calibration report script mirrors exact quality gate logic (aiRelevance filter + 0.55 threshold)
- All 7+ prospects evaluated with correct gate tiers documented
- Threshold constants confirmed or adjusted based on real data
- Distribution analysis committed as permanent calibration artifact
  </success_criteria>

<output>
After completion, create `.planning/phases/35-validation-and-calibration/35-01-SUMMARY.md`
</output>
