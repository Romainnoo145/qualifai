---
phase: 47-outreach-ui-simplification
plan: 02
type: execute
wave: 2
depends_on: ['47-01']
files_modified:
  - app/admin/outreach/page.tsx
  - server/routers/outreach.ts
autonomous: false
requirements:
  - UICL-03

must_haves:
  truths:
    - 'Non-email reminders (call, LinkedIn, WhatsApp) appear as a lightweight section within the Drafts Queue tab'
    - 'Reminders are visually distinct from email drafts — different styling, no email preview'
    - 'Admin can mark reminders as completed or skipped directly from the Drafts Queue'
  artifacts:
    - path: 'app/admin/outreach/page.tsx'
      provides: 'ReminderSection component rendered inside DraftQueue'
      contains: 'ReminderSection'
    - path: 'server/routers/outreach.ts'
      provides: 'getReminders query replacing getTouchTaskQueue'
  key_links:
    - from: 'app/admin/outreach/page.tsx'
      to: 'server/routers/outreach.ts'
      via: 'api.outreach.getReminders.useQuery'
      pattern: 'getReminders'
---

<objective>
Add a lightweight reminders section to the Drafts Queue tab showing non-email reminders (call, LinkedIn, WhatsApp) with complete/skip actions. Rename the backend endpoint from getTouchTaskQueue to getReminders for clarity.

Purpose: UICL-03 — non-email reminders should be visible inline in the Drafts Queue, not hidden in a separate tab that no longer exists.
Output: Drafts Queue shows email drafts AND non-email reminders in a single view, visually distinct.
</objective>

<execution_context>
@/home/klarifai/.claude/get-shit-done/workflows/execute-plan.md
@/home/klarifai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-outreach-ui-simplification/47-01-SUMMARY.md
@app/admin/outreach/page.tsx
@server/routers/outreach.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename getTouchTaskQueue to getReminders and add reminders section to DraftQueue</name>
  <files>app/admin/outreach/page.tsx, server/routers/outreach.ts</files>
  <action>
**Backend (server/routers/outreach.ts):**

1. Rename `getTouchTaskQueue` to `getReminders`. The query logic stays identical — it already queries reminder_open status items. Just rename the key in the router object.

**Frontend (app/admin/outreach/page.tsx):**

1. Create a `ReminderSection` component inside the file. This is a lightweight list of non-email reminders shown ABOVE the email drafts in the DraftQueue.

2. In `ReminderSection`:
   - Query `api.outreach.getReminders.useQuery({ status: 'open', limit: 50 })` (the renamed endpoint)
   - If no open reminders, render nothing (return null — no empty state needed)
   - Render a glass-card container with:
     - Header: Phone icon + "Reminders" title + count badge
     - Compact list of reminder items, each showing:
       - Contact name + company (as Link to `/admin/contacts/{id}`)
       - Channel pill (call/linkedin/whatsapp) using appropriate icon (Phone, Linkedin, MessageSquare)
       - Due date if present, with "Overdue" amber pill if past due
       - Subject/notes if present (single line, truncated)
       - Two small buttons: "Done" (emerald, Check icon) and "Skip" (slate, X icon)
     - Use `completeTouchTask` and `skipTouchTask` mutations (endpoint names stay — they handle reminders now)
     - On success, invalidate both `getReminders` and `getDecisionInbox`

3. Visual distinction from email drafts:
   - Use a distinct background: `bg-indigo-50/30 border-indigo-100` (soft indigo tint) vs the white glass-card of email drafts
   - Compact layout: single row per reminder (flex row), not the full card layout of drafts
   - No email preview section — just contact + channel + action buttons

4. Render `<ReminderSection />` inside the DraftQueue component, BEFORE the email drafts list (after the summary bar, before the manualReviewLeads section).

5. Add back the necessary imports removed in Plan 01: `Phone`, `Linkedin` (needed for channel icons in reminders). Keep `ListChecks` removed.
   </action>
   <verify>
   <automated>cd /home/klarifai/Documents/klarifai/projects/qualifai && npx tsc --noEmit 2>&1 | grep -v sitemap.test | head -20</automated>
   <manual>Check that reminders render inline in Drafts Queue with distinct styling</manual>
   </verify>
   <done>Non-email reminders appear as a compact, visually distinct section within the Drafts Queue. Complete/skip buttons work. No separate tab for reminders. Backend endpoint renamed from getTouchTaskQueue to getReminders.</done>
   </task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify 3-tab outreach page with inline reminders</name>
  <files>app/admin/outreach/page.tsx</files>
  <action>
Human verification of the complete outreach UI simplification:
- 3 tabs only (Drafts Queue, Replies, Sent History)
- No Multi-touch Tasks tab
- Non-email reminders shown inline in Drafts Queue with distinct indigo-tinted styling
- Complete/skip actions on reminders

Steps:

1. Navigate to http://localhost:9200/admin/outreach
2. Verify exactly 3 tab buttons: Drafts Queue, Replies, Sent History
3. On Drafts Queue tab: if reminders exist, they should appear as compact cards above email drafts with an indigo-tinted background
4. Reminder cards should show channel (call/linkedin/whatsapp), contact name, and Done/Skip buttons
5. Email drafts below should look unchanged (white cards with full email preview)
6. Click "Done" or "Skip" on a reminder — it should disappear from the list
7. Switch between tabs — all 3 should work correctly
   </action>
   <verify>Visual inspection at http://localhost:9200/admin/outreach — 3 tabs, reminders inline in Drafts Queue</verify>
   <done>User confirms 3-tab layout works correctly with inline reminders. Type "approved" or describe issues.</done>
   </task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes (excluding pre-existing sitemap.test.ts)
2. `grep -r "getTouchTaskQueue" server/ app/` returns nothing
3. `grep "getReminders" server/routers/outreach.ts` finds the renamed endpoint
4. `grep "ReminderSection" app/admin/outreach/page.tsx` finds the new component
5. Visual: Drafts Queue tab shows reminders (if any) above email drafts
</verification>

<success_criteria>

- Non-email reminders visible in Drafts Queue with distinct visual treatment
- Admin can complete/skip reminders from the Drafts Queue
- getTouchTaskQueue endpoint renamed to getReminders
- Complete 3-tab layout verified by user
  </success_criteria>

<output>
After completion, create `.planning/phases/47-outreach-ui-simplification/47-02-SUMMARY.md`
</output>
