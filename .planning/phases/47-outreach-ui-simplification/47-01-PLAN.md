---
phase: 47-outreach-ui-simplification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/admin/outreach/page.tsx
  - app/admin/contacts/[id]/page.tsx
autonomous: true
requirements:
  - UICL-01
  - UICL-02

must_haves:
  truths:
    - 'Outreach page shows exactly 3 tabs: Drafts Queue, Replies, Sent History'
    - 'No manual touch task creation form exists anywhere in the application'
    - 'No Multi-touch Tasks button or tab is visible'
  artifacts:
    - path: 'app/admin/outreach/page.tsx'
      provides: '3-tab outreach page without TouchTaskQueue'
      contains: "type View = 'queue' | 'replies' | 'sent'"
    - path: 'app/admin/contacts/[id]/page.tsx'
      provides: 'Contact detail without touch task invalidation'
  key_links:
    - from: 'app/admin/outreach/page.tsx'
      to: 'View type'
      via: 'tab toggle buttons'
      pattern: 'setView.*queue.*replies.*sent'
---

<objective>
Remove the Multi-touch Tasks tab and its entire TouchTaskQueue component from the outreach page, reducing from 4 tabs to 3 (Drafts Queue, Replies, Sent History). Also remove the dead manual task creation form and clean up related references in contact detail page.

Purpose: UICL-01 and UICL-02 — the cadence engine now auto-generates all follow-ups, making manual task management obsolete.
Output: Clean 3-tab outreach page with no trace of manual touch task creation.
</objective>

<execution_context>
@/home/klarifai/.claude/get-shit-done/workflows/execute-plan.md
@/home/klarifai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-automated-cadence-backend-cleanup/46-02-SUMMARY.md
@app/admin/outreach/page.tsx
@app/admin/contacts/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Multi-touch Tasks tab and TouchTaskQueue component</name>
  <files>app/admin/outreach/page.tsx</files>
  <action>
1. Change the View type from `'queue' | 'tasks' | 'replies' | 'sent'` to `'queue' | 'replies' | 'sent'`.

2. Remove the entire Multi-touch Tasks tab button (the button with `setView('tasks')` and `ListChecks` icon, lines ~53-61).

3. Remove the conditional render `view === 'tasks' ? <TouchTaskQueue /> :` from the view switching block (lines ~85-86). The result should be:

   ```
   {view === 'queue' ? <DraftQueue /> : view === 'replies' ? <ReplyInbox /> : <SentHistory />}
   ```

4. Delete the entire `TouchTaskQueue` function component (lines ~430-646) — approximately 217 lines including:
   - The state variables (contactId, channel, subject, notes, dueAt)
   - The contacts query
   - The tasks query (getTouchTaskQueue)
   - The completeTask and skipTask mutations
   - The submitTask stub
   - The entire JSX return (summary bar, creation form, task list)

5. Remove unused imports that were only used by TouchTaskQueue. After deletion, check which of these are no longer referenced elsewhere in the file and remove them: `Phone`, `Linkedin`, `ListChecks`, `MessageSquare` (check if MessageSquare is used in ReplyInbox — it IS, keep it). Also remove the `TouchChannel` type alias.

Keep: `MessageSquare` (used in ReplyInbox), all other imports used by remaining components.
Remove: `Phone`, `Linkedin`, `ListChecks`, `TouchChannel` type.
</action>
<verify>
<automated>cd /home/klarifai/Documents/klarifai/projects/qualifai && npx tsc --noEmit 2>&1 | grep -v sitemap.test | head -20</automated>
<manual>Confirm no 'tasks' view, no TouchTaskQueue, no ListChecks import</manual>
</verify>
<done>Outreach page has exactly 3 tabs (Drafts Queue, Replies, Sent History). No Multi-touch Tasks button or TouchTaskQueue component exists. No manual task creation form visible. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Clean up contact detail touch task references</name>
  <files>app/admin/contacts/[id]/page.tsx</files>
  <action>
1. Remove the `utils.outreach.getTouchTaskQueue.invalidate()` line from the mutation onSuccess handler (line ~64). Keep the other invalidations (contacts.get, outreach.getQueue, sequences.list).

2. Remove the comment `// queueTouchTask removed in 46-02 — manual task creation replaced by automated cadence` (line ~69) as it's no longer relevant — the entire feature is gone.

3. Verify no other references to getTouchTaskQueue or TouchTask remain in this file.
   </action>
   <verify>
   <automated>cd /home/klarifai/Documents/klarifai/projects/qualifai && npx tsc --noEmit 2>&1 | grep -v sitemap.test | head -20</automated>
   <manual>Confirm no getTouchTaskQueue references in contact detail</manual>
   </verify>
   <done>Contact detail page has zero references to getTouchTaskQueue or manual touch task creation. TypeScript compiles cleanly.</done>
   </task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes (excluding pre-existing sitemap.test.ts)
2. `grep -r "TouchTask\|touch.*task\|Multi-touch\|ListChecks" app/admin/outreach/page.tsx` returns nothing
3. `grep -r "getTouchTaskQueue" app/admin/contacts/` returns nothing
4. View type in outreach page is `'queue' | 'replies' | 'sent'` (3 values, not 4)
</verification>

<success_criteria>

- Outreach page renders 3 tab buttons only
- No TouchTaskQueue component exists in the codebase
- No manual task creation form or related UI exists
- TypeScript compiles without new errors
  </success_criteria>

<output>
After completion, create `.planning/phases/47-outreach-ui-simplification/47-01-SUMMARY.md`
</output>
