---
phase: 51-discover-page-redesign
plan: 03
type: execute
wave: 2
depends_on: [51-01]
files_modified:
  - components/public/partnership-discover-client.tsx
  - components/public/prospect-dashboard-atlantis-client.tsx
  - app/discover/[slug]/page.tsx
autonomous: true
requirements: [DISC-01, DISC-05]

must_haves:
  truths:
    - 'Legacy partnership-discover-client fallback still works for prospects without analysis'
    - 'No dead code from old wizard approach remains in the discover routing'
    - 'analysis-v1 MasterAnalysis type import removed from atlantis-discover-client'
  artifacts:
    - path: 'app/discover/[slug]/page.tsx'
      provides: 'Clean routing: v2 → flowing doc, v1 → legacy wizard, no analysis → loading state'
  key_links:
    - from: 'app/discover/[slug]/page.tsx'
      to: 'components/public/atlantis-discover-client.tsx'
      via: 'NarrativeAnalysis prop'
      pattern: 'NarrativeAnalysis'
---

<objective>
Clean up routing and remove dead code paths from the old wizard approach, ensuring the discover page routing is clean and handles all states correctly.

Purpose: Phase 50 changed the analysis engine to produce analysis-v2 (NarrativeAnalysis). Plan 01 rewrote the renderer. This plan ensures the routing layer is clean — no dangling v1 wizard code, clear fallback chain, and the partnership-discover-client (legacy fallback for prospects without analysis) still works.

Output: Clean routing in page.tsx, dead code removed.
</objective>

<execution_context>
@/home/klarifai/.claude/get-shit-done/workflows/execute-plan.md
@/home/klarifai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-discover-page-redesign/51-01-SUMMARY.md
@app/discover/[slug]/page.tsx
@components/public/partnership-discover-client.tsx
@components/public/prospect-dashboard-atlantis-client.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean up discover page routing and remove dead code</name>
  <files>app/discover/[slug]/page.tsx</files>
  <action>
Review and clean up the discover page routing in `app/discover/[slug]/page.tsx`:

1. **Routing priority** — verify the rendering chain is:
   a. ATLANTIS + analysis-v2 → `AtlantisDiscoverClient` (flowing document from Plan 01)
   b. ATLANTIS + analysis-v1 → legacy `AtlantisDiscoverClient` with v1 data (keep working for old prospects until they're rerun)
   c. ATLANTIS + no analysis → "Uw partnership analyse wordt voorbereid" loading state (existing)
   d. Non-ATLANTIS → `DashboardClient` (existing Klarifai flow, unchanged)

2. **Remove `parseMasterAnalysis`** — since Plan 01/Task 2 added `parseNarrativeAnalysis` and the v1 fallback, verify `parseMasterAnalysis` is only used for the v1 fallback path. If analysis-v1 prospects should just see the loading state instead (since they'll be rerun with the new pipeline), remove `parseMasterAnalysis` entirely and simplify the routing:
   - ATLANTIS + analysis-v2 → flowing document
   - ATLANTIS + no v2 → loading state
   - Non-ATLANTIS → DashboardClient

   Decision: Keep `parseMasterAnalysis` for now — there may be existing prospects with v1 data that should still render. Only remove when confirmed all prospects have been rerun.

3. **Clean up imports** — ensure no unused imports remain (no `PartnershipDiscoverClient` import in page.tsx since it's not directly used there).

4. **Verify metadata** — update the `discoverDescription` function to use language that matches the new flowing document approach: "Strategische partnership analyse voor {companyName}" instead of current text.
   </action>
   <verify>
   <automated>cd /home/klarifai/Documents/klarifai/projects/qualifai && npx tsc --noEmit 2>&1 | grep -c "discover.\*page\|atlantis-discover\|partnership-discover\|prospect-dashboard-atlantis" | xargs test 0 -eq</automated>
   <manual>All four routing paths work correctly</manual>
   </verify>
   <done>Discover page routing is clean with clear priority chain: v2 → v1 fallback → loading state → Klarifai flow. No dead imports or unused code paths.</done>
   </task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no new errors in any discover-related files
2. `npx eslint app/discover/[slug]/page.tsx components/public/atlantis-discover-client.tsx` — clean
3. Routing handles all four states correctly
4. No unused imports in page.tsx
</verification>

<success_criteria>

- Clean routing chain with no dead code
- All prospect states (v2 analysis, v1 analysis, no analysis, non-Atlantis) handled correctly
- No TypeScript or ESLint errors
  </success_criteria>

<output>
After completion, create `.planning/phases/51-discover-page-redesign/51-03-SUMMARY.md`
</output>
