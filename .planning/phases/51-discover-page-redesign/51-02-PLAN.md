---
phase: 51-discover-page-redesign
plan: 02
type: execute
wave: 2
depends_on: [51-01]
files_modified:
  - components/public/atlantis-discover-client.tsx
  - app/discover/[slug]/page.tsx
autonomous: true
requirements: [DISC-04]

must_haves:
  truths:
    - 'CTA section positions NDA signing as gateway to confidential dossier'
    - 'CTA copy uses language about vertrouwelijk dossier and geheimhouding'
    - 'CTA still provides Cal.com booking and direct contact options as secondary actions'
  artifacts:
    - path: 'components/public/atlantis-discover-client.tsx'
      provides: 'NDA-framed CTA section'
      contains: 'geheimhouding'
  key_links:
    - from: 'components/public/atlantis-discover-client.tsx'
      to: 'api.wizard'
      via: 'requestQuote mutation for NDA interest tracking'
      pattern: 'requestQuote'
---

<objective>
Replace the generic CTA section with an NDA-driven CTA that positions the next step as signing a geheimhoudingsverklaring to unlock a confidential partnership dossier.

Purpose: The CTA should create urgency and exclusivity — the prospect has seen a public-facing summary, but the real depth (specific financial models, allocation details, project timelines) is behind an NDA. This is the "Machiavellian chess" approach: give them enough to be intrigued, then gate the rest behind commitment.

Output: Updated CTA section in AtlantisDiscoverClient.
</objective>

<execution_context>
@/home/klarifai/.claude/get-shit-done/workflows/execute-plan.md
@/home/klarifai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-discover-page-redesign/51-01-SUMMARY.md
@components/public/atlantis-discover-client.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build NDA-driven CTA section</name>
  <files>components/public/atlantis-discover-client.tsx</files>
  <action>
Replace the placeholder CTA section from Plan 01 with an NDA-framed gateway section. This is purely UI/copy — no actual NDA e-sign flow (that's deferred to NDA-01 through NDA-04).

**CTA section structure** (max-w-3xl mx-auto):

1. **Divider** — a thin horizontal rule or `border-t border-slate-200` with generous `py-16` spacing above and below

2. **NDA gateway card** — centered, bg-white rounded-2xl p-10 shadow-sm border border-slate-100:
   - Small label at top: "Volgende stap" (text-[10px] font-black uppercase tracking-[0.2em] text-slate-400)
   - Heading: "Vertrouwelijk dossier beschikbaar" (text-2xl font-heading font-bold text-[#040026])
   - Body paragraph: "Wat u hierboven leest is een samenvatting op basis van openbare bronnen. Het volledige partnership dossier — met specifieke financiële modellen, allocatiedetails en projecttijdlijnen — is beschikbaar onder geheimhouding. Eén gesprek is genoeg om de scope te bepalen."
   - Primary CTA button: "Plan vertrouwelijk gesprek" — triggers handleBookCall (Cal.com). Style: w-full py-4 rounded-2xl text-[10px] font-black uppercase tracking-widest bg-[#040026] text-white hover:bg-[#1E1E4A] shadow-xl shadow-[#040026]/10
   - If no bookingUrl, show requestQuote button instead: "Verzoek toegang tot dossier" — triggers handleRequestQuote (existing quote request flow with confetti). Same button style but bg-[#EBCB4B] text-[#040026].
   - After quote requested, show confirmation: CheckCircle2 icon + "Uw verzoek is ontvangen. Wij nemen binnen 1 werkdag contact op voor de geheimhoudingsverklaring."

3. **Secondary contact options** — below the NDA card, smaller and understated:
   - Label: "Direct contact" (text-[10px] font-black uppercase tracking-[0.2em] text-slate-400)
   - Same contact buttons as before (WhatsApp, phone, email) but slightly smaller/more muted: text-xs instead of current size, bg-slate-50 borders

4. **Remove the old "Partnership Intake Aanvragen" card** — the NDA card replaces it entirely.

**Copy tone:** Controlled, fact-driven, implies there's more behind the gate. Not salesy — more like "we have the full picture, this is how you access it." Every sentence has a purpose.
</action>
<verify>
<automated>cd /home/klarifai/Documents/klarifai/projects/qualifai && npx tsc --noEmit 2>&1 | grep -c "atlantis-discover-client" | xargs test 0 -eq</automated>
<manual>CTA section uses NDA/geheimhouding language</manual>
</verify>
<done>CTA section frames next step as NDA gateway to confidential dossier. Primary action is Cal.com booking or quote request. Copy is controlled and fact-driven.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no new errors
2. `npx eslint components/public/atlantis-discover-client.tsx` — clean
3. CTA section contains "geheimhouding" and "vertrouwelijk dossier" language
4. Cal.com booking still works as primary action
5. Quote request with confetti still works as fallback
</verification>

<success_criteria>

- CTA positions NDA signing as the gateway to deeper content
- Copy implies depth behind the gate without being salesy
- All existing contact mechanisms (Cal.com, WhatsApp, phone, email) still functional
  </success_criteria>

<output>
After completion, create `.planning/phases/51-discover-page-redesign/51-02-SUMMARY.md`
</output>
