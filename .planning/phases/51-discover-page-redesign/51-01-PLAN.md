---
phase: 51-discover-page-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/public/atlantis-discover-client.tsx
autonomous: true
requirements: [DISC-01, DISC-02, DISC-03, DISC-05]

must_haves:
  truths:
    - 'Discover page renders as a flowing document with no step navigation or wizard UI'
    - 'Opening section shows prospect-specific hook from analysis.openingHook'
    - 'Executive summary paragraph is displayed prominently'
    - 'Narrative sections render with title + body + citations woven in'
    - 'SPV recommendations render as clean cards below narrative'
    - 'Visual design is clean, confident, boardroom-appropriate with Atlantis brand colors'
  artifacts:
    - path: 'components/public/atlantis-discover-client.tsx'
      provides: 'Flowing narrative document renderer for analysis-v2'
      min_lines: 150
  key_links:
    - from: 'components/public/atlantis-discover-client.tsx'
      to: 'lib/analysis/types.ts'
      via: 'NarrativeAnalysis import'
      pattern: 'import.*NarrativeAnalysis'
---

<objective>
Rewrite the AtlantisDiscoverClient from a 4-step wizard into a flowing boardroom document that renders NarrativeAnalysis (analysis-v2) content.

Purpose: The current wizard UI (Context → Waarom Nu → Samenwerking → Intake) fragments the narrative into disconnected steps. The new design presents everything as a single scrollable document — the way a boardroom briefing would be read.

Output: Rewritten `atlantis-discover-client.tsx` that renders analysis-v2 data as a flowing document.
</objective>

<execution_context>
@/home/klarifai/.claude/get-shit-done/workflows/execute-plan.md
@/home/klarifai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/analysis/types.ts
@components/public/atlantis-discover-client.tsx
@app/discover/[slug]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite AtlantisDiscoverClient as flowing boardroom document</name>
  <files>components/public/atlantis-discover-client.tsx</files>
  <action>
Completely rewrite `components/public/atlantis-discover-client.tsx` to render `NarrativeAnalysis` (analysis-v2) as a single-page flowing document. Remove all wizard/step logic (STEPS array, currentStep state, AnimatePresence, direction, goToStep, next, prev, step navigation header, bottom navigation footer, motion variants).

**Props change:**

- Change `analysis` prop type from `MasterAnalysis` to `NarrativeAnalysis` (import from `@/lib/analysis/types`)
- Keep all other props: companyName, industry, prospectSlug, projectBrandName, bookingUrl, whatsappNumber, phoneNumber, contactEmail, analysisDate

**Layout structure (single scrollable page):**

1. **Header** — sticky, same brand mark style (w-9 h-9 rounded-2xl bg-[#040026], yellow brandMark letter). Company name to the right. Keep existing pattern.

2. **Hero section** — centered, max-w-3xl mx-auto:
   - Badge: "Vertrouwelijk voorstel" pill (same style as current "Voorstel op maat" but text changed)
   - Title: `Partnership analyse — {companyName}` (text-4xl md:text-5xl font-heading font-black)
   - Opening hook: `analysis.openingHook` rendered as text-lg text-slate-600 leading-relaxed paragraph
   - Analysis date in small text below hook: `Opgesteld {analysisDate}`

3. **Executive summary** — max-w-3xl mx-auto, bg-white rounded-2xl p-8 shadow-sm:
   - Small label: "Samenvatting" (text-[10px] font-black uppercase tracking-[0.2em] text-slate-400)
   - Body: `analysis.executiveSummary` (text-base text-slate-700 leading-relaxed)

4. **Narrative sections** — max-w-3xl mx-auto, each section is a `<section>` with:
   - `section.title` as h2 (text-2xl font-heading font-bold text-[#040026])
   - `section.body` as paragraph(s) (text-base text-slate-700 leading-relaxed). The body may contain multiple paragraphs separated by newlines — split on `\n\n` and render each as a `<p>`.
   - `section.citations` rendered inline as small footnote references below the body. Style: text-xs text-slate-400 italic. Prefix with a thin divider line.
   - Use `space-y-12` between sections for generous whitespace.

5. **SPV recommendations** — max-w-3xl mx-auto, heading "Samenwerkingsroutes":
   - Each `spvRecommendation` as a clean card: bg-white rounded-2xl p-6 shadow-sm
   - `rec.spvName` as h3 font-semibold
   - `rec.relevanceNarrative` as text-sm text-slate-600
   - `rec.strategicTags` as small pills (bg-gray-100 text-xs px-2 py-0.5 rounded-full)
   - Use `space-y-4` between cards.

6. **CTA section** — placeholder for now (Plan 02 will build the NDA CTA). For this plan, render a simple centered section with:
   - Text: "Geinteresseerd in een vertrouwelijk gesprek?"
   - Cal.com book button (reuse existing handleBookCall logic with getCalApi)
   - Contact channels below (WhatsApp, phone, email — same pattern as current Step 3)

7. **Footer** — minimal: brand name in small text, analysis date

**Keep from current file:**

- Cal.com embed initialization (getCalApi, cal('ui', ...), handleBookCall)
- Session tracking (startSession, trackProgress, trackCall, requestQuote mutations via api.wizard.\*)
- WhatsApp URL construction
- Brand mark derivation

**Remove from current file:**

- STEPS array and step configuration
- currentStep, direction state
- AnimatePresence and motion.div wrapping
- Step navigation (header nav buttons, bottom prev/next buttons, mobile step dots)
- Progress bar
- TRIGGER_BORDER_COLORS map
- All step-conditional rendering (`{currentStep === 0 && ...}`)

**Styling rules:**

- Background: bg-[#F8F9FA] (same as current)
- Brand colors: #040026 (dark), #EBCB4B (yellow accent) — same as current, use CSS variables where they exist
- Typography: font-heading for headings, font-sans for body
- No framer-motion animations (remove import). Use CSS transitions only for hover states.
- Keep lucide-react icons but only import what's needed (remove ChevronRight, ChevronLeft, etc.)
- All text in Dutch

**Track session progress:** On mount, call startSession. Track scroll depth instead of steps — use an IntersectionObserver on section headings to report which sections the prospect has viewed. Call trackProgress with `{ sessionId, currentStep: sectionIndex, stepTimes }` when sections come into view (reuse existing mutation shape).
</action>
<verify>
<automated>cd /home/klarifai/Documents/klarifai/projects/qualifai && npx tsc --noEmit 2>&1 | grep -c "atlantis-discover-client" | xargs test 0 -eq</automated>
<manual>File compiles with zero errors related to atlantis-discover-client</manual>
</verify>
<done>AtlantisDiscoverClient renders NarrativeAnalysis as a single flowing document with hero, executive summary, narrative sections, SPV cards, and CTA — no wizard steps.</done>
</task>

<task type="auto">
  <name>Task 2: Update page.tsx to pass NarrativeAnalysis instead of MasterAnalysis</name>
  <files>app/discover/[slug]/page.tsx</files>
  <action>
Update `app/discover/[slug]/page.tsx` to handle analysis-v2 (NarrativeAnalysis) data:

1. **Add parseNarrativeAnalysis function** — similar to existing `parseMasterAnalysis` but validates analysis-v2 structure:
   - Check `obj.version === 'analysis-v2'`
   - Check `typeof obj.openingHook === 'string'`
   - Check `typeof obj.executiveSummary === 'string'`
   - Check `Array.isArray(obj.sections) && obj.sections.length > 0`
   - Check `Array.isArray(obj.spvRecommendations)`
   - Return `obj as unknown as NarrativeAnalysis` on success, null on failure

2. **Update the Atlantis rendering block** (around line 377):
   - Try `parseNarrativeAnalysis(prospectAnalysis.content)` first
   - If that succeeds, render `AtlantisDiscoverClient` with the NarrativeAnalysis
   - If that fails, fall back to `parseMasterAnalysis` for legacy analysis-v1 data
   - This ensures both old (analysis-v1) and new (analysis-v2) prospects render correctly

3. **Update import** — import `NarrativeAnalysis` from `@/lib/analysis/types` alongside `MasterAnalysis`

4. **Keep the "analysis being prepared" fallback** (the loading state for ATLANTIS prospects without any analysis) — unchanged.
   </action>
   <verify>
   <automated>cd /home/klarifai/Documents/klarifai/projects/qualifai && npx tsc --noEmit 2>&1 | grep -c "discover.\*page" | xargs test 0 -eq</automated>
   <manual>Page handles both analysis-v1 and analysis-v2 formats</manual>
   </verify>
   <done>Discover page routes analysis-v2 to new flowing document and falls back to wizard for legacy analysis-v1 data.</done>
   </task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no new errors in `atlantis-discover-client.tsx` or `app/discover/[slug]/page.tsx`
2. `npx eslint components/public/atlantis-discover-client.tsx app/discover/[slug]/page.tsx` — clean
3. AtlantisDiscoverClient accepts NarrativeAnalysis type (no MasterAnalysis reference)
4. No wizard step logic remains in the component
5. Page routing handles both v1 and v2 analysis formats
</verification>

<success_criteria>

- AtlantisDiscoverClient renders a single scrollable document (no steps/wizard)
- Opening hook, executive summary, narrative sections, SPV recommendations, and CTA all visible on one page
- Visual design uses boardroom-appropriate typography and spacing
- Both analysis-v1 (legacy) and analysis-v2 (new) prospects render without errors
  </success_criteria>

<output>
After completion, create `.planning/phases/51-discover-page-redesign/51-01-SUMMARY.md`
</output>
