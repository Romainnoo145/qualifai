---
phase: 51-discover-page-redesign
task: rework
total_tasks: 1
status: in_progress
last_updated: 2026-03-14T07:34:28.225Z
---

<current_state>
Phase 51 plans (01-03) executed and verified, but user feedback requires a rework: the new standalone scrolling AtlantisDiscoverClient needs to be reverted to use the existing 4-step wizard structure from DashboardClient, but with analysis-v2 narrative content inside each step.

The Phase 51 code commits are in git (8ffe6c0, 0f22eda, 6011590). The pipeline is working — Heijmans has analysis-v2 data in DB (3 sections, 2 SPV recommendations).
</current_state>

<completed_work>

- Phase 49: RAG Query Rebuild — AI-driven query builder + evidence-aware passage ranking (COMPLETE)
- Phase 50: Master Prompt Rebuild — narrative types + raw evidence prompt + pipeline wiring (COMPLETE, verified)
- Phase 51: Discover Page Redesign — 3 plans executed (COMPLETE, but needs rework per user feedback)
- Heijmans research run triggered and completed with analysis-v2 data
- Stuck Heijmans CRAWLING run reset to FAILED
  </completed_work>

<remaining_work>

- Rework AtlantisDiscoverClient to use DashboardClient's 4-step wizard structure with analysis-v2 content:
  - Step 0 (Context): Render openingHook + executiveSummary from NarrativeAnalysis
  - Step 1 (Kansen): Render narrative sections with body + citations
  - Step 2 (Waarde): Render SPV recommendations as cards
  - Step 3 (Intake): Keep the EXISTING contact page from DashboardClient exactly as-is (glass-card CTA, Cal.com booking, WhatsApp/phone/email, quote request)
- The approach: Add a `narrativeAnalysis?: NarrativeAnalysis` prop to DashboardClient. When present + isAtlantis, render v2 content in steps 0-2. Step 3 stays untouched. The PartnershipDiscoverClient continues to pass synthesized hypotheses for the old rendering path as fallback.
- Alternative approach: Keep the new AtlantisDiscoverClient as a separate component but add step navigation + the old contact page design back into it
- After rework: run Phase 52 (E2E Validation)
  </remaining_work>

<decisions_made>

- User confirmed the analysis-v2 narrative content quality is excellent ("heel vet", "echt goede info")
- User explicitly wants the 4-step navigation structure back — the single scroll lost overview
- Contact/Intake page (step 3) from old DashboardClient was "gewoon mooi" — must return as-is
- The NDA gateway language from Plan 51-02 (geheimhouding, vertrouwelijk dossier) can be integrated into the existing step 3 design
- v7.0 vision updated: keep wizard structure, fill with narrative content (not "replace wizard with scroll")
  </decisions_made>

<blockers>
- None — just needs implementation time in fresh context
</blockers>

<context>
Key files to read when resuming:
- components/public/prospect-dashboard-client.tsx — The DashboardClient with 4-step wizard (lines 110-122 for ATLANTIS_STEPS, 488-628 for step 0, 631-873 for step 1, 877-1050 for step 2, 1053-1202 for step 3)
- components/public/partnership-discover-client.tsx — The PartnershipDiscoverClient wrapper that transforms partnership data into DashboardClient props (still works, used as fallback for non-v2 prospects)
- app/discover/[slug]/page.tsx — Routing: currently routes ATLANTIS+v2 to new standalone component; needs to route back to DashboardClient/PartnershipDiscoverClient with narrativeAnalysis prop
- lib/analysis/types.ts — NarrativeAnalysis, NarrativeSection, SPVRecommendation types (the v2 output)

The cleanest approach is probably: add `narrativeAnalysis` prop to DashboardClient, and when isAtlantis + narrativeAnalysis is present, render the v2 content in each step's isAtlantis branch. Step 3 stays completely untouched. The page.tsx routing sends narrativeAnalysis as a prop instead of branching to a separate component.
</context>

<next_action>
Start with: Read DashboardClient step 0 Atlantis branch (line ~488-628), then add narrativeAnalysis prop and replace the atlantisContext-based rendering with analysis-v2 narrative content. Work through steps 0→1→2, leave step 3 untouched. Update page.tsx routing to pass narrativeAnalysis as prop instead of branching to standalone component.
</next_action>
