---
phase: 46-automated-cadence-backend-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - '46-01'
files_modified:
  - server/routers/outreach.ts
  - server/routers/admin.ts
autonomous: true
requirements:
  - BKCL-01
  - BKCL-02
  - BKCL-03

must_haves:
  truths:
    - 'queueTouchTask endpoint no longer exists -- calling it would return an error'
    - 'touch_open, touch_done, touch_skipped status values are gone from the outreach router'
    - 'completeTouchTask and skipTouchTask handle reminder_open/reminder_done/reminder_skipped for cadence reminders'
    - 'Admin dashboard queries use reminder_open instead of touch_open'
  artifacts:
    - path: 'server/routers/outreach.ts'
      provides: 'Cleaned outreach router without queueTouchTask, with refactored complete/skip for reminders'
    - path: 'server/routers/admin.ts'
      provides: 'Dashboard query using reminder_open instead of touch_open'
  key_links:
    - from: 'server/routers/outreach.ts completeTouchTask'
      to: 'lib/cadence/engine.ts REMINDER_STATUS_OPEN'
      via: 'import of reminder status constants'
      pattern: 'REMINDER_STATUS_OPEN|reminder_open'
    - from: 'server/routers/admin.ts'
      to: 'lib/cadence/engine.ts REMINDER_STATUS_OPEN'
      via: 'dashboard query filter'
      pattern: 'reminder_open'
---

<objective>
Remove the manual touch task creation endpoint and refactor completion/skip endpoints to work with the new reminder status values from the cadence engine.

Purpose: The old manual task flow (queueTouchTask creating touch_open, completeTouchTask/skipTouchTask transitioning to touch_done/touch_skipped) is replaced by automated cadence reminders. This plan removes dead code and updates the remaining endpoints to use reminder_open/reminder_done/reminder_skipped.

Output: Cleaned outreach router, updated admin dashboard query.
</objective>

<execution_context>
@/home/klarifai/.claude/get-shit-done/workflows/execute-plan.md
@/home/klarifai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-automated-cadence-backend-cleanup/46-01-SUMMARY.md
@server/routers/outreach.ts
@server/routers/admin.ts
@lib/cadence/engine.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove queueTouchTask and refactor complete/skip for reminders</name>
  <files>server/routers/outreach.ts</files>
  <action>
Modify server/routers/outreach.ts:

1. **Import reminder status constants** from `@/lib/cadence/engine`:
   Add `REMINDER_STATUS_OPEN, REMINDER_STATUS_DONE, REMINDER_STATUS_SKIPPED` to the existing import from `@/lib/cadence/engine`.

2. **Remove the old status constants** at the top of the file:
   Delete `TOUCH_TASK_STATUS_OPEN = 'touch_open'`, `TOUCH_TASK_STATUS_DONE = 'touch_done'`, `TOUCH_TASK_STATUS_SKIPPED = 'touch_skipped'`.

3. **Delete queueTouchTask endpoint entirely** (lines ~1069-1127). This removes the manual task creation endpoint (BKCL-01).

4. **Refactor getTouchTaskQueue**:
   - Rename it to `getReminderQueue` for clarity (or keep the name if renaming would break too many UI references -- check app/admin/outreach/page.tsx usage. Since Phase 47 will kill the UI that calls it, keep the name `getTouchTaskQueue` for now to avoid breaking the Multi-touch Tasks tab that still exists until Phase 47).
   - Update the status filter: replace `TOUCH_TASK_STATUS_OPEN` with `REMINDER_STATUS_OPEN`, `TOUCH_TASK_STATUS_DONE` with `REMINDER_STATUS_DONE`, `TOUCH_TASK_STATUS_SKIPPED` with `REMINDER_STATUS_SKIPPED`.
   - Update the all-status array to use the new constants.
   - Update the summary counters to use the new constants.

5. **Refactor completeTouchTask**:
   - Change the status guard from `task.status !== TOUCH_TASK_STATUS_OPEN` to `task.status !== REMINDER_STATUS_OPEN`. Update the error message to "Only open reminders can be completed".
   - Change the update from `status: TOUCH_TASK_STATUS_DONE` to `status: REMINDER_STATUS_DONE`.
   - Keep the cadence evaluation fire-and-forget call (this still works).
   - Keep the outreachStep update to SENT status (this still works).

6. **Refactor skipTouchTask**:
   - Change the status guard from `task.status !== TOUCH_TASK_STATUS_OPEN` to `task.status !== REMINDER_STATUS_OPEN`. Update error message to "Only open reminders can be skipped".
   - Change the update from `status: TOUCH_TASK_STATUS_SKIPPED` to `status: REMINDER_STATUS_SKIPPED`.
   - Keep the cadence evaluation and outreachStep update logic.

7. **Clean up**: Remove the `TOUCH_TASK_CHANNELS` constant if no longer used (check: getTouchTaskQueue uses it in the where clause -- keep it if still referenced, just rename or leave as-is). Remove unused `TouchTaskChannel` type if no longer referenced.
   </action>
   <verify>
   <automated>cd /home/klarifai/Documents/klarifai/projects/qualifai && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
   <manual>Verify queueTouchTask is gone and complete/skip use reminder statuses</manual>
   </verify>
   <done>queueTouchTask endpoint deleted. completeTouchTask and skipTouchTask use reminder_open/reminder_done/reminder_skipped. getTouchTaskQueue queries use new status values. No touch_open/touch_done/touch_skipped string literals remain in outreach.ts.</done>
   </task>

<task type="auto">
  <name>Task 2: Update admin dashboard query and run full check</name>
  <files>server/routers/admin.ts</files>
  <action>
1. **Update admin.ts dashboard query**: Around line 773, the action queue dashboard queries `status: 'touch_open'` for open touch tasks. Change this to `status: 'reminder_open'`. Import `REMINDER_STATUS_OPEN` from `@/lib/cadence/engine` for type safety, or just use the string literal `'reminder_open'` to match the existing pattern in admin.ts (which uses string literals for other statuses like 'draft').

2. **Search for any remaining touch_open references**: Run a project-wide search for `touch_open`, `touch_done`, `touch_skipped` in .ts/.tsx files (excluding .planning/ and node_modules). If any are found in:
   - `app/admin/outreach/page.tsx` -- the TouchTaskQueue component will be deleted in Phase 47, but its mutation calls reference `completeTouchTask` and `skipTouchTask` which still exist (just with new internal status logic). The UI doesn't reference status strings directly -- it calls the endpoint and the endpoint handles the status. So no UI change needed here.
   - Any other file -- update to use the new status values.

3. **Run npm run check** to verify TypeScript compilation, linting, and formatting all pass.
   </action>
   <verify>
   <automated>cd /home/klarifai/Documents/klarifai/projects/qualifai && npm run check 2>&1 | tail -20</automated>
   </verify>
   <done>Admin dashboard uses reminder_open for action queue. npm run check passes (type-check + lint + format). No touch_open/touch_done/touch_skipped string literals remain in server/ or lib/ directories (only in .planning/ docs and tests).</done>
   </task>

</tasks>

<verification>
1. `npm run check` passes (type-check + lint + format)
2. `npx vitest run lib/cadence/engine.test.ts` -- all tests still pass
3. `grep -rn 'touch_open\|touch_done\|touch_skipped' server/ lib/ --include='*.ts'` returns zero matches (only .planning/ and test expectations should reference old values)
4. `grep -n 'queueTouchTask' server/routers/outreach.ts` returns zero matches
5. `grep -n 'reminder_open' server/routers/outreach.ts server/routers/admin.ts` returns matches
</verification>

<success_criteria>

- queueTouchTask endpoint is completely removed from the outreach router
- touch_open, touch_done, touch_skipped status values are not used anywhere in server/ or lib/ code
- completeTouchTask transitions from reminder_open to reminder_done
- skipTouchTask transitions from reminder_open to reminder_skipped
- Admin dashboard action queue queries for reminder_open
- npm run check passes with zero errors
  </success_criteria>

<output>
After completion, create `.planning/phases/46-automated-cadence-backend-cleanup/46-02-SUMMARY.md`
</output>
