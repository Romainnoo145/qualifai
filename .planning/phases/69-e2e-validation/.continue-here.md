---
phase: 69-e2e-validation
status: ready_to_close
last_updated: 2026-04-22T17:00:00.000Z
---

## Wat er gedaan is (2026-04-22)

### DB opgeschoond

- Alle EvidenceItem, ProspectAnalysis, ResearchRun gewist (3129 items, 70 runs, 13 analyses)
- Prospect records intact gelaten

### Pipeline: replace ipv append

- `lib/research-executor.ts` — verse run (`!existingRunId`) verwijdert nu eerst alle oude ResearchRuns per prospect (cascadeert EvidenceItems). Geen ophoping meer.

### STB-specifieke use cases verwijderd

- 3 use cases met Offorte API / window installation company content uit DB verwijderd
- Waren door matchProofs ook aan Marfa gekoppeld

### Marfa verse pipeline gedraaid

- Scrapling hergestart: `docker compose up scrapling -d`
- `scripts/tmp-rerun-marfa.ts` → 28 schone evidence items (na dedup + relevance gate)
- `scripts/tmp-rerun-analysis-marfa.ts` → nieuwe analyse gegenereerd zonder hernieuwd scrapen

### Brochure design verbeterd (`components/features/analyse/analyse-brochure.tsx`)

- Punchline als hero heading op alle sectiepagina's (was: sectietitel 2x herhaald)
- Dubbele punt opgelost (`replace(/\.+$/, '')` voor GoldDot)
- `derivePillars`: alleen korte zinnen (<72 chars) als title, anders leeg → geen afgekapte halve zinnen
- `PillarCard`: title conditioneel gerenderd

## Wat er nog moet

### 1. Commit alles

```
/commit
```

Wijzigingen: research-executor.ts (replace logic), analyse-brochure.tsx (design fixes), scripts/tmp-rerun-\*.ts (validation scripts)

### 2. v10.0 milestone sluiten

```
/gsd:complete-milestone
```

### 3. Optioneel: andere prospects rerunnen

STB-kozijnen, Mujjo, Nedri, Heijmans hebben nu geen evidence meer. Scrapling draait. Gewoon via admin UI een nieuwe research run triggeren per prospect.

## Bestanden gewijzigd (niet gecommit)

- `lib/research-executor.ts` — replace-not-append logic (regel ~687)
- `components/features/analyse/analyse-brochure.tsx` — punchline als hero, pillar fix
- `scripts/tmp-rerun-analysis-marfa.ts` — nieuw script (analysis-only rerun)
- `scripts/tmp-rerun-marfa.ts` — bestaand script (gebruikt)
- `.planning/config.json` — modified (M in git status)

## State

- Dev server: draait op port 9200 (PID 156207, log: /tmp/qualifai-dev.log)
- Scrapling: draait op port 3010
- DB: alleen Marfa heeft evidence (28 items) en analyse
- Alle andere prospects: leeg, klaar voor verse run
