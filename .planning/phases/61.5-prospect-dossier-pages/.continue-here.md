---
phase: 61.5-prospect-dossier-pages
task: wire evidence + analyse sub-pages, dim outreach/resultaten, fix activity toggles
status: not_started
last_updated: 2026-04-20T12:45:00.000Z
---

<current_state>
Phase 61.4 (QA fix sweep) is ~80% done. Plans 01-03 executed, but checkpoint verification not formally approved. Phase 61.5 added to roadmap for prospect dossier pages.

The admin prospect detail page has 4 dossier sidebar links (Evidence, Analyse, Outreach, Resultaten) that navigate to sub-route pages — but all 4 are "Coming Soon" stubs. Evidence (427 items) and Analyse (ProspectAnalysis narrative) have real data in the DB. Outreach and Resultaten have 0 data.

The activity feed has toggles (Evidence, Outreach, Offertes) that match no events — `buildActivityFromRuns` only creates RUN/ENRICH/QUALITY event types.
</current_state>

<completed_work>

## Session 2026-04-20 (this session)

### QA Sweep

- Full-app QA with `/qa-only` skill: 13 issues found, health score 68/100
- Report: `.gstack/qa-reports/qa-report-localhost-9200-2026-04-20.md`

### Phase 61.4 Replan + Execution

- Removed 5 stale design sweep plans, updated CONTEXT.md with QA findings
- 3 new plans created with skill-driven verification (`/browse`, `/carrot`, `/qa`, `/design-review`)

**Plan 01 — Backend Wiring (executed):**

- Start voorstel: creates draft quote via `quotes.create` → navigates to `/admin/quotes/[newId]`
- 4 dossier sidebar links wired to sub-routes (evidence/analyse/outreach/resultaten)
- Dashboard START OUTREACH: `stopPropagation` + navigates to outreach sub-route
- Action bar buttons confirmed wired to tRPC mutations

**Plan 02 — Prompt Engineering (executed):**

- `VisualType` + `VisualData` types added to `lib/analysis/types.ts`
- Master prompt updated to request punchline + visualType + visualData per section
- `SectionVisual` component in analyse-brochure.tsx (quote/comparison/signals/stats renderers)
- Bronnen count fixed: uses `rawEvidenceItems.length` not filtered count
- Citations hidden behind collapsible `<details>` element

**Plan 03 — Visual Polish (task 1 executed, checkpoint pending):**

- BrandChrome `companyName` passed on all 7 offerte pages
- Cover text rendered dynamically from `quote.onderwerp` with responsive sizing

### Bugfixes (outside plans)

- **Enrichment null-guard**: `buildEnrichmentData` in `server/routers/admin.ts` now filters null values — Apollo no-coverage fallback no longer overwrites existing companyName/industry/etc
- **Old quotes/new page deleted**: `app/admin/prospects/[id]/quotes/new/page.tsx` removed (replaced by quote detail flow)
- **Mujjo data restored**: companyName, industry, city, country, employeeCount restored in DB
- **Prospect detail layout**: full-width description, compact stat cards (26px), symmetric spacing, border-t on stat section

### Phase 61.5 Added

- Roadmap entry + directory created
- Scope: evidence page, analyse page, dim outreach/resultaten, activity toggles

</completed_work>

<remaining_work>

## Phase 61.4 — Finish Checkpoint

- Plan 03 Task 2 (human verification checkpoint) not formally approved
- Run `/qa` for regression test + health score comparison
- Run `/design-review` on brochure surfaces
- Then approve or describe issues

## Phase 61.5 — Prospect Dossier Pages (NEW)

### 1. Evidence sub-page (`app/admin/prospects/[id]/evidence/page.tsx`)

- Query `EvidenceItem` by prospectId (already 427 items for Mujjo)
- Group by `sourceType` (WEBSITE 278, REVIEWS 113, NEWS 25, CAREERS 9, LINKEDIN 2)
- Render: source type header, item title/snippet, confidence score, link to source
- Use existing SubRouteShell wrapper (already has tabs + back nav)

### 2. Analyse sub-page (`app/admin/prospects/[id]/analyse/page.tsx`)

- Query `ProspectAnalysis` by prospectId (1 record for Mujjo, gemini-2.5-pro)
- Render narrative sections with the new `SectionVisual` component (reuse from analyse-brochure)
- Show punchline + visual data if available, fall back to body text

### 3. Outreach/Resultaten links — dim/disable

- In prospect detail page (`app/admin/prospects/[id]/page.tsx`), style the Outreach and Resultaten DossierLink components as disabled/dimmed
- Add "coming soon" text or tooltip
- Don't remove the links — they should be visible but clearly not active yet

### 4. Activity feed toggles

- In `buildActivityFromRuns` (`app/admin/prospects/[id]/page.tsx`), add EVIDENCE events
- Query evidence items count per run and create events: "46 evidence items verzameld"
- OUTREACH and QUOTE toggles: hide if no events exist, or show count badge (0)

### Key files

- `app/admin/prospects/[id]/evidence/page.tsx` — currently ComingSoonBlock stub
- `app/admin/prospects/[id]/analyse/page.tsx` — currently ComingSoonBlock stub
- `app/admin/prospects/[id]/_shared/sub-route-shell.tsx` — shared wrapper (already works)
- `app/admin/prospects/[id]/page.tsx` — activity feed + dossier links
- `server/routers/admin.ts` — may need new queries for evidence list / analysis detail

### tRPC queries needed

- `admin.listEvidence` or similar — return evidence items grouped by sourceType for a prospect
- `admin.getAnalysis` or reuse existing — return ProspectAnalysis with sections

### Suggested approach

- `/gsd:plan-phase 61.5` — one plan, ~4 tasks, single wave
- Or just do it directly — scope is clear, no ambiguity

</remaining_work>

<decisions_made>

- Evidence + Analyse pages should show real data (it exists in DB)
- Outreach + Resultaten stay as dimmed/disabled links (0 data)
- Activity toggles should only show types that have events
- Start voorstel creates draft quote inline (no separate form page)
- Enrichment must never null-out existing data (null-guard added)
- Old quotes/new page deleted — replaced by quote detail flow
  </decisions_made>

<blockers>
- Apollo API key expired (401) — blocks company/contact search enrichment (external action, pending todo)
- Prompt engineering changes need a re-run of master analyzer to test visual data output (existing analyses don't have visualType/visualData)
</blockers>

<context>
Key learnings this session:
1. The action buttons (Re-enrich, Nieuwe run) can trigger data loss if Apollo has no coverage — fixed with null-guard
2. Old pages that were "replaced" but not deleted cause confusion — always delete replaced pages
3. "Coming Soon" stubs are worse than no links when the data already exists in the DB
4. The browse tool loses session between bash calls — chain everything in one call for auth-dependent flows

The user was frustrated this session about:

- Plans not accounting for available skills (40+)
- Old/stale pages still being linked
- Action buttons silently destroying data
- Sub-pages being empty when data exists
  </context>

<next_action>

1. `/gsd:plan-phase 61.5` — or just build directly, scope is small
2. After 61.5: re-run `/qa` to get updated health score
3. Then formally close 61.4 checkpoint
4. Consider re-running master analyzer for one prospect to test visual data output
   </next_action>
