---
phase: 44-discover-rendering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/discover/[slug]/page.tsx
autonomous: true
requirements:
  - DISC-01
must_haves:
  truths:
    - 'ATLANTIS prospect with analysis data routes to AtlantisDiscoverClient with analysis prop'
    - 'ATLANTIS prospect without analysis data shows a professional pending state'
    - 'KLARIFAI prospects route unchanged to DashboardClient'
  artifacts:
    - path: 'app/discover/[slug]/page.tsx'
      provides: 'Server-side analysis fetch and routing logic'
      contains: 'prospectAnalysis'
  key_links:
    - from: 'app/discover/[slug]/page.tsx'
      to: 'prisma.prospectAnalysis'
      via: 'findFirst query'
      pattern: "prospectAnalysis\\.findFirst"
    - from: 'app/discover/[slug]/page.tsx'
      to: 'components/public/atlantis-discover-client.tsx'
      via: 'dynamic import or direct import'
      pattern: 'AtlantisDiscoverClient'
---

<objective>
Fetch ProspectAnalysis server-side and route ATLANTIS prospects to the new AI-rendered discover client or pending state.

Purpose: Establish the data flow from DB to client component, ensuring analysis content is available as props without client-side tRPC calls.
Output: Updated page.tsx with three-way routing: ATLANTIS+analysis → new client, ATLANTIS+no analysis → pending, KLARIFAI → unchanged.
</objective>

<execution_context>
@/home/klarifai/.claude/get-shit-done/workflows/execute-plan.md
@/home/klarifai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-discover-rendering/44-CONTEXT.md
@.planning/phases/43-ai-master-analysis/43-01-SUMMARY.md
@.planning/phases/43-ai-master-analysis/43-02-SUMMARY.md
@app/discover/[slug]/page.tsx
@lib/analysis/types.ts
@components/public/partnership-discover-client.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server-side analysis fetch and three-way routing</name>
  <files>app/discover/[slug]/page.tsx</files>
  <action>
Modify the server component `app/discover/[slug]/page.tsx`:

1. **Add ProspectAnalysis fetch**: After the existing prospect query, add a Prisma query to fetch the latest ProspectAnalysis for this prospect:

```ts
const prospectAnalysis = await prisma.prospectAnalysis.findFirst({
  where: { prospectId: prospect.id },
  orderBy: { createdAt: 'desc' },
  select: { content: true, createdAt: true },
});
```

2. **Parse analysis content**: If `prospectAnalysis` exists, parse `prospectAnalysis.content` (stored as JSON) and validate it has the MasterAnalysis shape (version === 'analysis-v1', context, triggers, tracks). Import the `MasterAnalysis` type from `@/lib/analysis/types` and create a type-safe parse function. Use a simple runtime check — do NOT use zod here to keep the page light.

3. **Three-way routing in the JSX**:
   - If `projectType === 'ATLANTIS'` AND valid parsed analysis exists → render `<AtlantisDiscoverClient>` (import from `@/components/public/atlantis-discover-client`). Pass these props:
     - `companyName`, `industry`, `prospectSlug` (from existing dashboardProps)
     - `analysis` (the parsed MasterAnalysis object)
     - `projectBrandName` (from existing dashboardProps)
     - `bookingUrl`, `whatsappNumber`, `phoneNumber`, `contactEmail` (from env vars, same as existing)
     - `analysisDate` (formatted from prospectAnalysis.createdAt)
   - If `projectType === 'ATLANTIS'` AND no valid analysis → render a minimal pending state inline (NOT a separate component). This should be a centered card with the brand mark, company name, and Dutch text: "Uw analyse wordt momenteel voorbereid. U ontvangt bericht zodra deze gereed is." styled professionally with the same `min-h-screen bg-[#F8F9FA]` background.
   - If not ATLANTIS → render existing `<DashboardClient>` unchanged.

4. **Import cleanup**: Add import for `AtlantisDiscoverClient` from `@/components/public/atlantis-discover-client`. Keep the existing `PartnershipDiscoverClient` import for now (it will be removed in a future cleanup, not this phase). The existing ATLANTIS branch that renders `PartnershipDiscoverClient` should be replaced by the new three-way logic.

5. **Metadata update**: In `generateMetadata`, update the ATLANTIS title to use "Partnership Analyse" instead of "Partnership readiness" for the new AI-driven content.

6. **Remove unused code**: The `parsePartnershipSnapshot` function and related helpers (that build `PartnershipDiscoverSnapshot`) can stay for now — they are only removed when partnership-discover-client.tsx is retired.

IMPORTANT: The `AtlantisDiscoverClient` component does not exist yet — it will be created in Plan 02. Create a minimal placeholder file at `components/public/atlantis-discover-client.tsx` that exports the component with the correct props interface but renders a simple "Loading..." div. This ensures TypeScript compiles. Plan 02 will replace this with the full implementation.
</action>
<verify>
<automated>cd /home/klarifai/Documents/klarifai/projects/qualifai && npx tsc --noEmit 2>&1 | head -30</automated>
<manual>Check that page.tsx has three-way routing for ATLANTIS+analysis, ATLANTIS+pending, KLARIFAI</manual>
</verify>
<done> - ATLANTIS prospects with ProspectAnalysis route to AtlantisDiscoverClient with parsed MasterAnalysis as prop - ATLANTIS prospects without ProspectAnalysis show Dutch pending message - KLARIFAI prospects render DashboardClient unchanged - TypeScript compiles clean
</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. page.tsx fetches ProspectAnalysis from DB and routes based on projectType + analysis presence
3. Pending state renders professional Dutch message for ATLANTIS prospects without analysis
</verification>

<success_criteria>

- ProspectAnalysis is fetched server-side and parsed into MasterAnalysis type
- Three routing paths work: ATLANTIS+analysis → new client, ATLANTIS+no analysis → pending, other → DashboardClient
- No client-side tRPC calls for analysis data
  </success_criteria>

<output>
After completion, create `.planning/phases/44-discover-rendering/44-01-SUMMARY.md`
</output>
