---
phase: 42-extraction-matrix
plan: 02
type: execute
wave: 2
depends_on: ['42-01']
files_modified:
  - lib/rag/retriever.ts
  - lib/research-executor.ts
  - components/features/prospects/intent-signals-section.tsx
  - app/admin/prospects/[id]/page.tsx
  - server/routers/research.ts
autonomous: true
requirements: [EXTR-02, EXTR-03]

must_haves:
  truths:
    - 'RAG retrieval queries are constructed from intent variables instead of keyword-stuffed profile fragments'
    - 'RAG queries auto-scope to relevant SPVs based on intent variable content'
    - 'Intent variables are visible in admin prospect detail between evidence and analysis sections'
    - 'Sparse extraction shows a yellow warning badge in admin'
    - 'Same RAG chunk appearing in multiple category queries is deduplicated with highest similarity kept'
  artifacts:
    - path: 'lib/rag/retriever.ts'
      provides: 'buildRagQueryInputsFromIntent function that constructs per-category semantic queries'
      exports: ['buildRagQueryInputsFromIntent']
    - path: 'lib/research-executor.ts'
      provides: 'Updated Atlantis flow using intent-driven RAG queries'
    - path: 'components/features/prospects/intent-signals-section.tsx'
      provides: 'Admin UI component showing intent variable categories as cards'
      exports: ['IntentSignalsSection']
    - path: 'app/admin/prospects/[id]/page.tsx'
      provides: 'Intent Signals tab integrated into prospect detail page'
    - path: 'server/routers/research.ts'
      provides: 'tRPC endpoint to fetch intent extraction for a research run'
  key_links:
    - from: 'lib/research-executor.ts'
      to: 'lib/rag/retriever.ts'
      via: 'buildRagQueryInputsFromIntent replaces buildRagQueryInputs for Atlantis'
      pattern: 'buildRagQueryInputsFromIntent'
    - from: 'components/features/prospects/intent-signals-section.tsx'
      to: 'server/routers/research.ts'
      via: 'tRPC query for intent extraction data'
      pattern: "api\\.research\\.getIntentExtraction"
    - from: 'app/admin/prospects/[id]/page.tsx'
      to: 'components/features/prospects/intent-signals-section.tsx'
      via: 'IntentSignalsSection rendered in tab'
      pattern: 'IntentSignalsSection'
---

<objective>
Replace keyword-stuffed RAG queries with intent-variable-driven semantic queries, and add admin UI for inspecting extracted intent variables.

Purpose: Intent-driven queries retrieve more relevant Atlantis passages by asking targeted questions per category (e.g., ESG signals search for green hydrogen capacity, not a blob of profile keywords). Admin visibility enables quality oversight.
Output: Smarter RAG retrieval + "Intent Signals" section in admin prospect detail.
</objective>

<execution_context>
@/home/klarifai/.claude/get-shit-done/workflows/execute-plan.md
@/home/klarifai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-extraction-matrix/42-CONTEXT.md
@.planning/phases/42-extraction-matrix/42-01-SUMMARY.md
@lib/rag/retriever.ts
@lib/research-executor.ts
@lib/extraction/types.ts
@app/admin/prospects/[id]/page.tsx
@server/routers/research.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Intent-driven RAG query builder + research executor integration</name>
  <files>
    lib/rag/retriever.ts
    lib/research-executor.ts
  </files>
  <action>
    1. In `lib/rag/retriever.ts`, create `buildRagQueryInputsFromIntent` function:
       - Input: `intentVars: IntentVariables`, `prospectProfile: { companyName: string, industry: string | null, spvName: string | null }`
       - For each populated core category in `intentVars.categories`, generate ONE targeted semantic query:
         - `sector_fit`: Use the top signals to query for sector-specific Atlantis capabilities. Example: if signals mention "steel production" and "hydrogen", query becomes "[companyName] steel green hydrogen production capacity Europe's Gate partnership"
         - `operational_pains`: Use pain signals to query for Atlantis solutions addressing those pains. Example: "stikstof vergunning bottleneck fast-track permitting Europe's Gate"
         - `esg_csrd`: Use ESG signals to query for compliance/sustainability documentation. Example: "CSRD compliance green hydrogen emissions reduction"
         - `investment_growth`: Use growth/investment signals to query for financing and ROI passages. Example: "capex project finance subsidy EIB returns"
         - `workforce`: Use workforce signals to query for workforce development documentation. Example: "workforce training labor capacity scaling"
       - For each query, extract the 2-3 most relevant signal descriptions and combine with the company name and "Europe's Gate" / "Atlantis" context. Keep queries under 200 chars (concise, not keyword-stuffed).
       - Auto-scope SPV: For each category, examine the signal content to infer relevant SPV names. Use the `inferSectorQueryPack` pattern as reference for sector-to-SPV mapping, but derive from actual signal content rather than hardcoded industry regex. If signals mention specific infrastructure (e.g., "datacenter", "steel"), tag the query with a relevant SPV hint.
       - Also process `intentVars.extras` categories the same way.
       - Return `RagQueryInput[]` with `workflowTag` set to the category name (e.g., `'intent-sector_fit'`, `'intent-esg_csrd'`).
       - Retrieve top 5 passages per query.
       - Keep the existing `buildRagQueryInputs` function intact (it is still used for non-intent fallback).

    2. In `lib/research-executor.ts`, inside the Atlantis block:
       - After the `extractIntentVariables` call (added in Plan 01), check if `intentVars` was successfully extracted.
       - If intent variables are available and have at least 2 populated categories:
         - Call `buildRagQueryInputsFromIntent(intentVars, { companyName, industry, spvName })` INSTEAD of the existing `buildRagQueryInputs` call.
         - Pass the resulting query inputs to `retrieveRagPassages` as before.
       - If intent extraction failed or is too sparse (< 2 populated categories):
         - Fall back to the existing `buildRagQueryInputs` call (graceful degradation).
       - Add diagnostic entry indicating which query strategy was used: `intent-driven (N categories)` or `keyword-fallback`.

    3. Deduplication is already handled by `retrieveRagPassages` (it uses `byChunkId` Map with highest similarity wins). Verify the existing dedup logic handles the new workflowTags correctly — no change should be needed since dedup is by chunkId not by tag. Add a `categories` array to the workflowTag-based dedup if same chunk appears across category queries so downstream consumers know all matching categories.

  </action>
  <verify>
    <automated>cd /home/klarifai/Documents/klarifai/projects/qualifai && npx tsc --noEmit --pretty 2>&1 | head -40</automated>
    <manual>Research executor uses intent-driven queries for Atlantis with keyword fallback</manual>
  </verify>
  <done>Atlantis RAG queries are constructed from intent variable categories with targeted semantic queries. Keyword-stuffed fallback remains for sparse extractions. Deduplication works across category queries.</done>
</task>

<task type="auto">
  <name>Task 2: Admin Intent Signals UI section</name>
  <files>
    server/routers/research.ts
    components/features/prospects/intent-signals-section.tsx
    app/admin/prospects/[id]/page.tsx
  </files>
  <action>
    1. In `server/routers/research.ts`, add a `getIntentExtraction` query:
       - Input: `{ runId: string }` (research run ID)
       - Query: `db.intentExtraction.findUnique({ where: { researchRunId: runId } })`
       - Return the intent extraction record or null
       - Use the same auth/middleware pattern as other queries in this router

    2. Create `components/features/prospects/intent-signals-section.tsx`:
       - Props: `{ runId: string | null }` (latest research run ID)
       - Fetch intent extraction via `api.research.getIntentExtraction.useQuery({ runId }, { enabled: !!runId })`
       - Parse `variables` JSON back into `IntentVariables` type
       - Render category cards using glass-card pattern (consistent with existing sections):
         - Each core category gets a card with:
           - Category name as heading (human-readable: "Sector Fit", "Operational Pains", "ESG / CSRD", "Investment & Growth", "Workforce")
           - List of extracted signals, each showing:
             - Signal description text
             - Confidence badge (0-1 score, colored: green >= 0.7, amber >= 0.4, red < 0.4)
             - Source type chip (WEBSITE, NEWS, LINKEDIN, etc.)
             - Source URL as external link icon
           - Empty category: show subtle "No signals extracted" text
         - Extra categories (from `intentVars.extras`) rendered the same way below the core cards
       - If `sparse` is true: show a yellow warning badge/chip at the top: "Sparse extraction — fewer than 3 categories populated"
       - If no intent extraction exists for this run: show "No intent extraction available" message
       - Read-only — no edit actions (autopilot with oversight pattern)
       - Use compact layout (p-4 not p-10), consistent with user preference

    3. In `app/admin/prospects/[id]/page.tsx`:
       - Add "Intent Signals" to the TABS array, positioned between 'evidence' and 'analysis': `{ id: 'intent-signals', label: 'Intent Signals' }`
       - Import and render `IntentSignalsSection` in the tab content area, passing `latestRunId`
       - Use the same `hidden` CSS pattern for tab switching (keep mounted, hide inactive)
       - Only show the Intent Signals tab for Atlantis prospects (check `prospect.data.project.projectType === 'ATLANTIS'`). For Klarifai prospects, filter it out of the rendered tabs.

  </action>
  <verify>
    <automated>cd /home/klarifai/Documents/klarifai/projects/qualifai && npm run check 2>&1 | tail -20</automated>
    <manual>Intent Signals tab appears in admin prospect detail for Atlantis prospects, showing category cards with signals</manual>
  </verify>
  <done>Admin prospect detail shows "Intent Signals" tab (Atlantis only) between Evidence and Analysis. Category cards display extracted signals with confidence scores, source types, and source links. Sparse extraction shows yellow warning badge.</done>
</task>

</tasks>

<verification>
- `npm run check` passes (type-check + lint + format)
- `npx prisma validate` passes
- Intent-driven RAG queries are used for Atlantis prospects with sufficient intent variables
- Keyword-stuffed fallback is used when intent extraction fails or is sparse
- Admin prospect detail shows Intent Signals tab for Atlantis prospects only
- Category cards render with confidence scores, source types, and links
</verification>

<success_criteria>

- RAG retrieval queries are constructed from intent variables with one targeted query per populated category
- SPV auto-scoping derives from intent variable content, not hardcoded industry regex
- Deduplication handles cross-category chunk matches correctly
- Admin "Intent Signals" section shows category cards between Evidence and Analysis tabs
- Sparse extraction warning is visible at a glance
- Existing Klarifai prospect pipeline is completely unaffected (no Intent Signals tab, no extraction calls)
  </success_criteria>

<output>
After completion, create `.planning/phases/42-extraction-matrix/42-02-SUMMARY.md`
</output>
