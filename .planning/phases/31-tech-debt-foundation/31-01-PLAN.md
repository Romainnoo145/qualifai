---
phase: 31-tech-debt-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/research-executor.ts
  - components/public/prospect-dashboard-client.tsx
  - app/discover/[slug]/page.tsx
  - lib/workflow-engine.ts
  - lib/evidence-scorer.ts
  - lib/ai/constants.ts
autonomous: true
requirements:
  - DEBT-01
  - DEBT-02
  - DEBT-05
  - MODEL-02

must_haves:
  truths:
    - 'SERP cache re-read after overwrite is gone — deepCrawl block uses only useSerpFromSourceSet flag'
    - 'logoUrl prop does not exist on DashboardClient interface and is not passed at any call site'
    - 'No import statements appear after export statements in workflow-engine.ts'
    - 'All Gemini model calls reference GEMINI_MODEL_FLASH constant from lib/ai/constants.ts, no inline gemini-2.0-flash strings remain in workflow-engine.ts or evidence-scorer.ts'
  artifacts:
    - path: 'lib/research-executor.ts'
      provides: 'SERP cache fix — single pre-read guard'
      contains: 'useSerpFromSourceSet'
    - path: 'lib/ai/constants.ts'
      provides: 'Shared Gemini model constant'
      exports: ['GEMINI_MODEL_FLASH']
    - path: 'lib/workflow-engine.ts'
      provides: 'Clean imports at top, Gemini constant imported'
      contains: 'GEMINI_MODEL_FLASH'
    - path: 'lib/evidence-scorer.ts'
      provides: 'Gemini constant imported'
      contains: 'GEMINI_MODEL_FLASH'
  key_links:
    - from: 'lib/workflow-engine.ts'
      to: 'lib/ai/constants.ts'
      via: 'import GEMINI_MODEL_FLASH'
      pattern: 'import.*GEMINI_MODEL_FLASH.*from.*ai/constants'
    - from: 'lib/evidence-scorer.ts'
      to: 'lib/ai/constants.ts'
      via: 'import GEMINI_MODEL_FLASH'
      pattern: 'import.*GEMINI_MODEL_FLASH.*from.*ai/constants'
---

<objective>
Fix four independent tech debt items: SERP cache double-read bug, unused logoUrl prop, import ordering anomaly, and Gemini model string extraction to shared constant with 2.5-flash upgrade.

Purpose: Establish a clean baseline before hypothesis prompt work in Phase 32. These are all isolated, zero-risk code surgery items that touch different subsystems.
Output: Four fixes applied, `npx tsc --noEmit` still passes with zero errors, shared GEMINI_MODEL_FLASH constant ready for Phase 33 model selection.
</objective>

<execution_context>
@/home/klarifai/.claude/get-shit-done/workflows/execute-plan.md
@/home/klarifai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/31-tech-debt-foundation/31-RESEARCH.md

@lib/research-executor.ts
@components/public/prospect-dashboard-client.tsx
@app/discover/[slug]/page.tsx
@lib/workflow-engine.ts
@lib/evidence-scorer.ts
@lib/ai/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix SERP cache double-read and remove unused logoUrl prop</name>
  <files>
    lib/research-executor.ts
    components/public/prospect-dashboard-client.tsx
    app/discover/[slug]/page.tsx
  </files>
  <action>
**DEBT-01 — SERP cache re-read fix in research-executor.ts:**

Inside the `deepCrawl` block (around lines 316-330), there is a redundant `db.researchRun.findUnique` call that re-reads the run AFTER `inputSnapshot` was already overwritten. This produces stale/wrong data.

Fix: Remove the second `findUnique` call and any `serpCache` extraction from it. The `isCacheValid` guard should use ONLY `useSerpFromSourceSet` (computed from `priorSourceSet` BEFORE the overwrite at lines 146-150). The variable `priorSourceSet` is already in scope.

The fixed logic:

```typescript
const isCacheValid = useSerpFromSourceSet;
```

If `isCacheValid` is true, reconstruct `serpResult` from `priorSourceSet` (already in memory):

```typescript
const serpResult: SerpDiscoveryResult = isCacheValid
  ? {
      reviewUrls:
        priorSourceSet?.urls
          .filter((u) => u.provenance === 'serp')
          .map((u) => u.url) ?? [],
      jobUrls: [],
      discoveredAt:
        priorSourceSet?.serpDiscoveredAt ?? new Date().toISOString(),
    }
  : await discoverSerpUrls({
      companyName: prospect.companyName,
      domain: prospect.domain,
    });
```

Remove the `existingSnapshot = findUnique(...)` and `serpCache = extractSerpCache(existingSnapshot)` lines. Leave the `serpCache` field in the snapshot write (backward compat is harmless).

**DEBT-02 — Remove unused logoUrl prop:**

1. In `components/public/prospect-dashboard-client.tsx`: Remove `logoUrl: string | null` from the `DashboardClientProps` interface (around line 72). Do NOT touch the component's destructure or render — `logoUrl` is not used there.
2. In `app/discover/[slug]/page.tsx`: Remove `logoUrl={prospect.logoUrl}` from the `<DashboardClient>` JSX call site (around line 247).

Run `npx tsc --noEmit` after both to confirm no remaining references.
</action>
<verify>
<automated>cd /home/klarifai/Documents/klarifai/projects/qualifai && npx tsc --noEmit 2>&1 | tail -5</automated>
<manual>Confirm no findUnique re-read in deepCrawl block, no logoUrl in DashboardClient interface</manual>
</verify>
<done> - research-executor.ts deepCrawl block has no second findUnique call; isCacheValid uses only useSerpFromSourceSet - DashboardClientProps interface has no logoUrl field - app/discover/[slug]/page.tsx does not pass logoUrl to DashboardClient - npx tsc --noEmit passes with zero errors
</done>
</task>

<task type="auto">
  <name>Task 2: Fix import ordering and extract Gemini model constant</name>
  <files>
    lib/workflow-engine.ts
    lib/evidence-scorer.ts
    lib/ai/constants.ts
  </files>
  <action>
**DEBT-05 — Import ordering fix in workflow-engine.ts:**

Lines 540-544 have two import statements AFTER the `export { computeTrafficLight }` re-export at line 539:

```typescript
import {
  MIN_AVERAGE_CONFIDENCE,
  PAIN_CONFIRMATION_MIN_SOURCES,
} from '@/lib/quality-config';
import type { TrafficLight } from '@/lib/quality-config';
```

Move both import statements to the top import block of workflow-engine.ts (after the other imports, before any code). Remove lines 540-544. The re-export at line 539 stays in place.

**MODEL-02 — Gemini model constant extraction + upgrade:**

1. Create or update `lib/ai/constants.ts` — add:

```typescript
/** Gemini Flash model identifier. Phase 33 will extend with model selector. */
export const GEMINI_MODEL_FLASH = 'gemini-2.5-flash' as const;
```

Note: `lib/ai/constants.ts` may already exist with other content — ADD the export, do not overwrite existing content.

2. In `lib/workflow-engine.ts` (lines 723 and 1422): Replace the inline `'gemini-2.0-flash'` string with `GEMINI_MODEL_FLASH`. Add `import { GEMINI_MODEL_FLASH } from '@/lib/ai/constants';` to the import block.

3. In `lib/evidence-scorer.ts` (line 141): Replace the inline `'gemini-2.0-flash'` string with `GEMINI_MODEL_FLASH`. Add the same import.

**IMPORTANT:** Do NOT touch `generate-wizard.ts`, `generate-outreach.ts`, `vault-reader.ts`, or `codebase-analyzer.ts` — those are out of scope per user decision. Only the 2 files above (workflow-engine.ts, evidence-scorer.ts) get the constant.

Run `grep -rn "gemini-2.0-flash" lib/workflow-engine.ts lib/evidence-scorer.ts` after to confirm zero remaining inline strings in these two files.
</action>
<verify>
<automated>cd /home/klarifai/Documents/klarifai/projects/qualifai && npx tsc --noEmit 2>&1 | tail -5 && grep -rn "gemini-2.0-flash" lib/workflow-engine.ts lib/evidence-scorer.ts; echo "grep exit: $?"</automated>
<manual>Confirm GEMINI_MODEL_FLASH exported from lib/ai/constants.ts, imported in workflow-engine.ts and evidence-scorer.ts; no import-after-export in workflow-engine.ts</manual>
</verify>
<done> - lib/ai/constants.ts exports GEMINI_MODEL_FLASH = 'gemini-2.5-flash' - workflow-engine.ts imports GEMINI_MODEL_FLASH and uses it at both getGenerativeModel calls (no inline strings) - evidence-scorer.ts imports GEMINI_MODEL_FLASH and uses it at its getGenerativeModel call (no inline string) - No import statements appear after any export statement in workflow-engine.ts - npx tsc --noEmit passes with zero errors
</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npx tsc --noEmit` — zero errors
2. `grep -rn "gemini-2.0-flash" lib/workflow-engine.ts lib/evidence-scorer.ts` — no matches
3. `grep -n "logoUrl" components/public/prospect-dashboard-client.tsx` — no matches in interface
4. `grep -n "findUnique" lib/research-executor.ts` — confirm no re-read inside deepCrawl block (the initial pre-read at the top is fine)
</verification>

<success_criteria>

- npx tsc --noEmit passes with zero errors
- SERP cache deepCrawl block has single pre-read guard (useSerpFromSourceSet only)
- DashboardClient interface has no logoUrl prop
- Gemini model string is 'gemini-2.5-flash' via shared constant in workflow-engine.ts and evidence-scorer.ts
- workflow-engine.ts has no import-after-export anomaly
  </success_criteria>

<output>
After completion, create `.planning/phases/31-tech-debt-foundation/31-01-SUMMARY.md`
</output>
