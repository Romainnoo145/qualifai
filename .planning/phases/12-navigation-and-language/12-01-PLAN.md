---
phase: 12-navigation-and-language
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/admin/layout.tsx
autonomous: true

must_haves:
  truths:
    - 'Sidebar shows exactly 6 items: Dashboard, Companies, Campaigns, Draft Queue, Use Cases, Signals — in that order'
    - 'No nav group labels remain — items are a flat list without section headings'
    - 'Removed pages (Hypotheses, Research runs, Product Briefs, Contacts) are still accessible via URL but not in the sidebar'
    - 'Mobile sidebar matches desktop sidebar — same 6 items, same order'
  artifacts:
    - path: 'app/admin/layout.tsx'
      provides: 'Restructured sidebar with 6 nav items'
      contains: 'Dashboard.*Companies.*Campaigns.*Draft Queue.*Use Cases.*Signals'
  key_links:
    - from: 'app/admin/layout.tsx'
      to: '/admin, /admin/prospects, /admin/campaigns, /admin/outreach, /admin/use-cases, /admin/signals'
      via: 'navItems array href values'
      pattern: 'href.*admin'
---

<objective>
Restructure the admin sidebar from 10 items in 3 groups to exactly 6 items in a flat list.

Purpose: The current sidebar has 10 items across 3 groups (Overview, Intelligence, Outreach). The user only uses 6 pages regularly. The rest are accessible through prospect detail or are redundant. A clean 6-item nav makes the app self-explanatory — the admin can predict where to find anything.

Output: Updated `app/admin/layout.tsx` with 6 sidebar items in the correct order.
</objective>

<execution_context>
@/home/klarifai/.claude/get-shit-done/workflows/execute-plan.md
@/home/klarifai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/admin/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure sidebar to 6 flat items</name>
  <files>app/admin/layout.tsx</files>
  <action>
In the `AdminShell` component, replace the current `navItems` array (3 groups with 10 items total) with a flat array of 6 items in this exact order:

1. Dashboard — href: `/admin`, icon: `LayoutDashboard`
2. Companies — href: `/admin/prospects`, icon: `Building2`
3. Campaigns — href: `/admin/campaigns`, icon: `FolderKanban`
4. Draft Queue — href: `/admin/outreach`, icon: `Mail`
5. Use Cases — href: `/admin/use-cases`, icon: `BookOpen`
6. Signals — href: `/admin/signals`, icon: `Zap`

Remove items: Hypotheses (`/admin/hypotheses`), Research runs (`/admin/research`), Product Briefs (`/admin/briefs`), Contacts (`/admin/contacts`). These pages still exist for direct URL access — only remove from nav.

Change the navItems type from `{ group: string; items: NavItem[] }[]` to `NavItem[]` (flat array, no groups).

Update the desktop sidebar `<nav>` rendering to iterate directly over navItems without group headers. Remove the group `<p>` labels ("Overview", "Intelligence", "Outreach") and the `space-y-10` group spacing. Use `space-y-1` for the flat list of NavLink items.

Update the mobile sidebar `<nav>` rendering the same way — same flat list, no group headers.

Remove unused icon imports: `Users` (Contacts), `Beaker` (Research), `FileText` (Briefs), `Lightbulb` (Hypotheses). Keep all other imports.
</action>
<verify>
Run `npx tsc --noEmit` — no type errors. Visually verify: the navItems array contains exactly 6 objects, no group wrappers exist, no group label rendering exists.
</verify>
<done>
Sidebar renders 6 items (Dashboard, Companies, Campaigns, Draft Queue, Use Cases, Signals) in a flat list with no section headers. Mobile sidebar matches. Removed pages still accessible via direct URL.
</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. `grep -c "href.*admin" app/admin/layout.tsx` shows exactly 6 nav hrefs (plus 1 for the logo link)
3. The strings "Hypotheses", "Research runs", "Product Briefs", "Contacts" do not appear in layout.tsx
4. The strings "Overview", "Intelligence", "Outreach" (group labels) do not appear in layout.tsx
</verification>

<success_criteria>

- Sidebar shows exactly 6 items in order: Dashboard, Companies, Campaigns, Draft Queue, Use Cases, Signals
- No group headers visible
- Mobile nav matches desktop nav
- TypeScript compiles without errors
  </success_criteria>

<output>
After completion, create `.planning/phases/12-navigation-and-language/12-01-SUMMARY.md`
</output>
