---
phase: 12-navigation-and-language
plan: 02
type: execute
wave: 2
depends_on: ['01']
files_modified:
  - app/admin/layout.tsx
  - components/features/prospects/command-center.tsx
  - app/admin/prospects/[id]/page.tsx
  - app/admin/campaigns/page.tsx
  - app/admin/briefs/page.tsx
  - app/admin/settings/page.tsx
  - app/admin/contacts/[id]/page.tsx
autonomous: true

must_haves:
  truths:
    - "The terms 'Loss Map', 'Call Prep', 'Nodes', and 'Sprint Intelligence' do not appear in any sidebar item, page heading, button label, or tab label"
    - 'All replaced labels use plain Dutch/English equivalents that a non-technical admin understands'
    - 'Settings page section headings use plain language instead of military/spy jargon'
    - "Campaigns page uses 'Companies' instead of 'Nodes' and 'Campaign Processing' instead of 'Sprint Intelligence'"
  artifacts:
    - path: 'components/features/prospects/command-center.tsx'
      provides: 'Plain-language status and button labels replacing Loss Map'
    - path: 'app/admin/prospects/[id]/page.tsx'
      provides: 'Renamed tabs and buttons (no Loss Map, no Call Prep)'
    - path: 'app/admin/campaigns/page.tsx'
      provides: 'Renamed labels (no Nodes, no Sprint Intelligence)'
    - path: 'app/admin/briefs/page.tsx'
      provides: 'Renamed heading and labels (no Loss Map, no intelligence jargon)'
    - path: 'app/admin/settings/page.tsx'
      provides: 'Plain-language section headings and stat labels'
    - path: 'app/admin/layout.tsx'
      provides: 'Login subtitle without jargon'
    - path: 'app/admin/contacts/[id]/page.tsx'
      provides: 'Button label without Loss Map'
  key_links:
    - from: 'app/admin/prospects/[id]/page.tsx'
      to: 'command-center.tsx'
      via: 'props (latestLossMap prop name stays — internal, not user-facing)'
      pattern: 'latestLossMap'
---

<objective>
Replace all user-facing jargon terms with plain language across admin page headings, buttons, tabs, status labels, and section titles.

Purpose: The admin should never see internal technical terms or spy-movie jargon. Every label should be immediately understandable. This is TERM-01: the first terminology pass covering sidebar, headings, buttons, tabs, and toasts. Variable names and prop names are internal and stay as-is — only user-visible strings change.

Output: Updated UI strings in 7 files. No logic changes, no prop renames, no API changes.
</objective>

<execution_context>
@/home/klarifai/.claude/get-shit-done/workflows/execute-plan.md
@/home/klarifai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/admin/layout.tsx
@components/features/prospects/command-center.tsx
@app/admin/prospects/[id]/page.tsx
@app/admin/campaigns/page.tsx
@app/admin/briefs/page.tsx
@app/admin/settings/page.tsx
@app/admin/contacts/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace jargon in prospect detail and command center</name>
  <files>app/admin/prospects/[id]/page.tsx, components/features/prospects/command-center.tsx</files>
  <action>
**In `components/features/prospects/command-center.tsx`:**
- Line ~157: `Loss Map ready` → `Workflow Report ready`
- Line ~160: `No Loss Map` → `No Workflow Report`
- Line ~185: `Regenerate Loss Map` → `Regenerate Report`
- Line ~185: `Generate Loss Map` → `Generate Report`

**In `app/admin/prospects/[id]/page.tsx`:**

- Tab label ~line 216: `label: 'Loss Map'` → `label: 'Workflow Report'`
- Tab label ~line 221: `label: 'Call Prep'` → `label: 'Call Brief'`
- Button ~line 1024: `Generate Loss Map` → `Generate Report`
- Text ~line 1049: `Run a research cycle first to unlock loss map generation.` → `Run research first to generate the workflow report.`
- Text ~line 1092: `No Workflow Loss Map generated yet.` → `No workflow report generated yet.`

Only change user-visible string literals. Do NOT rename variables, props, function names, or internal identifiers (e.g., `latestLossMap`, `generateLossMap`, `lossMap`, `runLossMapGeneration`, `CallPrepTab`, `LossMapTab` all stay as-is).
</action>
<verify>
`npx tsc --noEmit` passes. `grep -i "Loss Map" components/features/prospects/command-center.tsx app/admin/prospects/[id]/page.tsx` returns only variable/prop names and comments, never user-facing strings between `>` and `<` or inside template literals displayed to user. `grep "'Call Prep'" app/admin/prospects/[id]/page.tsx` returns 0 matches.
</verify>
<done>
Prospect detail tabs say "Workflow Report" and "Call Brief" instead of "Loss Map" and "Call Prep". Command center buttons and status text use "Report" language. No user-facing jargon remains in these two files.
</done>
</task>

<task type="auto">
  <name>Task 2: Replace jargon in campaigns, briefs, contacts, settings, and login</name>
  <files>app/admin/campaigns/page.tsx, app/admin/briefs/page.tsx, app/admin/settings/page.tsx, app/admin/contacts/[id]/page.tsx, app/admin/layout.tsx</files>
  <action>
**In `app/admin/campaigns/page.tsx`:**
- Line ~132: `Propagate Nodes to Sprint` → `Add Companies to Campaign`
- Line ~140: `Select Tactical Campaign` → `Select Campaign`
- Line ~209: `{campaign._count.campaignProspects} Nodes` → `{campaign._count.campaignProspects} Companies`
- Line ~223: `Sprint Intelligence` → `Campaign Processing`

**In `app/admin/briefs/page.tsx`:**

- Line ~40: heading `Workflow Loss Maps` → `Workflow Reports`
- Line ~44: badge `High Fidelity Assets` → `Generated Reports`
- Line ~90: `Select an intelligence asset` → `Select a report`
- Line ~93: `Choose a loss map from the list to preview high-fidelity data.` → `Choose a report from the list to preview.`
- Line ~101: `Loading Intelligence...` → `Loading report...`
- Line ~126: `Export High-Res PDF` → `Export PDF`
- Line ~136: `Research Lineage:` → `Research Run:`

**In `app/admin/settings/page.tsx`:**

- Section heading `Operational Footprint` → `Usage Overview`
- Stat label `Credits Deployed` → `Credits Used`
- Stat label `Target Domains` → `Companies`
- Stat label `Personnel Nodes` → `Contacts`
- Stat label `Market Signals` → `Signals`
- Section heading `Tactical Offloading` → `Data Export`
- Button label `Export Organizations` → `Export Companies`
- Button label `Export Personnel` → `Export Contacts`
- Helper text: `Enrichment exports include granular environmental data. Authorization required for all tactical offloads.` → `Export enriched company and contact data as CSV.`
- Section heading `System Architecture` → `System Info`
- Label `Platform Core` → `Platform`
- Value `Qualifai Deep Search` → `Qualifai`
- Label `Discovery Engine` → `Enrichment`
- Label `Linguistic Model` → `AI Model`
- Label `Dispatch Layer` → `Email Delivery`
- Value `Resend Protocol` → `Resend`
- Label `Temporal Sync` → `Scheduling`
- Value `Sequential Sync Offline` → `Not configured`

**In `app/admin/contacts/[id]/page.tsx`:**

- Line ~149: `No Loss Map Yet` → `No Report Yet`

**In `app/admin/layout.tsx`:**

- Line ~84: `Precision Intelligence Engine` → `Sales Intelligence`
  </action>
  <verify>
  `npx tsc --noEmit` passes. Run these checks:
- `grep -n "Sprint Intelligence" app/admin/campaigns/page.tsx` → 0 matches
- `grep -n "Nodes\|Propagate\|Tactical Campaign" app/admin/campaigns/page.tsx` → 0 matches (in user-facing strings)
- `grep -n "Loss Map" app/admin/briefs/page.tsx` → 0 matches
- `grep -n "Operational Footprint\|Tactical Offloading\|Personnel Nodes\|Target Domains" app/admin/settings/page.tsx` → 0 matches
- `grep -n "Precision Intelligence" app/admin/layout.tsx` → 0 matches
  </verify>
  <done>
  All jargon terms removed from campaigns, briefs, settings, contacts, and login pages. Every user-facing label uses plain language understandable by a non-technical admin. No variable or API names changed.
  </done>
  </task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. Run a global jargon check across all admin pages:
   - `grep -rn "Loss Map\|Call Prep\|Sprint Intelligence\|Personnel Nodes\|Precision Intelligence\|Tactical Offloading\|Operational Footprint\|High Fidelity\|intelligence asset\|Research Lineage\|Deep Search\|Dispatch Layer\|Temporal Sync\|Linguistic Model\|Propagate Nodes\|Tactical Campaign" app/admin/ components/features/prospects/command-center.tsx` — only matches in variable/prop names and non-user-facing code, never between JSX tags or in string literals rendered to user
3. The four explicitly banned terms ("Loss Map", "Call Prep", "Nodes" as standalone label, "Sprint Intelligence") appear nowhere in user-visible text across sidebar, headings, buttons, tabs
</verification>

<success_criteria>

- Zero instances of banned jargon in user-visible text (sidebar, headings, buttons, tabs, labels, toasts)
- All replacements use plain, intuitive language
- No logic changes, no prop renames, no API changes
- TypeScript compiles without errors
  </success_criteria>

<output>
After completion, create `.planning/phases/12-navigation-and-language/12-02-SUMMARY.md`
</output>
