---
phase: 61.4-admin-design-system-sweep + analyse brochure
task: frontend complete, needs QA + prompt engineering
total_tasks: 3 tracks (QA, prompt, integration)
status: frontend_built_needs_qa_and_prompt_work
last_updated: 2026-04-20T11:30:00.000Z
---

<current_state>
Two major frontend surfaces are now built:

1. **Admin (16 pages)** — A3 Ink & Paper Editorial, fully redesigned, committed
2. **Analyse brochure (`/analyse/[slug]`)** — Dark navy click-through brochure with video cover, section pages, kansen, contact. STRUCTURE is done but CONTENT is wrong.

The analyse brochure has a fundamental problem: the AI generates text-only output, but the brochure needs visual-first content (charts, scores, comparisons). The master prompt needs to be updated to generate structured visual data per section before the brochure pages can work properly.

The offerte brochure (`/offerte/[slug]`) was already complete from the previous session.
</current_state>

<completed_work>

## This Session (14 commits, 2026-04-19/20)

### Route Rename

- `/discover/[slug]` → `/analyse/[slug]` with 301 redirects from `/discover/` and `/voor/`
- All references updated (prospect-url.ts, notifications, tests)
- next.config.ts redirects configured

### Shared Infrastructure

- `lib/brochure-tokens.ts` — extracted brand tokens (NAVY, GOLD, etc.) shared between offerte + analyse
- `components/shared/brochure-chrome.tsx` — BrandChrome (now accepts `companyName` prop → shows `K ✕ Mujjo`), ProgressIndicator, GeometricBackdrop, NextArrow, BackArrow
- Offerte brochure refactored to use shared components

### Analyse Brochure Component

- `app/analyse/[slug]/layout.tsx` — Sora font, metadata, noindex
- `components/features/analyse/analyse-brochure.tsx` (~1300 lines) containing:
  - Video cover with dynamic stats overlay (bronnen/brontypen/inzichten, fades in after 2s)
  - 3 section page layouts: Split, Pillars, Quote (selected by content)
  - Kansen page with recommendation cards (SPV + UseCase support)
  - Contact page (centered hero + 3 pill CTA buttons)
  - Keyboard ← → navigation, session tracking
  - Responsive breakpoints (tablet 1024px, mobile 768px with scroll)

### Server Wiring

- `app/analyse/[slug]/page.tsx` renders AnalyseBrochure when analysis-v2 exists
- Falls back to old DashboardClient for prospects without analysis
- `server/routers/wizard.ts` — step limit raised from 5 to 20

### Design Assets

- `public/video/klarifai-analyse.mp4` — "OUTSMART YOUR INDUSTRY" video (15s, from Canva)
- Design spec: `docs/superpowers/specs/2026-04-19-analyse-page-redesign.md`
- Implementation plan: `docs/superpowers/plans/2026-04-19-analyse-page-redesign.md`
- Mockups: `.superpowers/brainstorm/` (two sessions)

</completed_work>

<remaining_work>

## Track 1: AI Prompt Engineering (BLOCKING)

The analyse brochure currently dumps AI-generated text on pages. Nobody reads this. The AI must generate VISUAL-READY structured data per section.

**What needs to change in the master prompt:**

- Each section needs a `punchline` (one-sentence headline for the brochure)
- Each section needs a `visualType`: "quote" | "comparison" | "signals" | "stats"
- Each section needs `visualData` with structured content matching the visualType
- The AI chooses the visual type based on what evidence it found

**Files to modify:**

- `lib/analysis/types.ts` — extend NarrativeSection type with punchline + visualType + visualData
- `lib/analysis/master-prompt.ts` — update Klarifai prompt to request visual-ready output
- `lib/analysis/master-analyzer.ts` — handle new output format
- `components/features/analyse/analyse-brochure.tsx` — render visual elements instead of text dumps

**Suggested skill:** `/gsd:plan-phase` for structured prompt engineering work

## Track 2: Full QA Sweep

The entire frontend needs testing — admin pages, offerte, analyse. Things that are likely broken or rough:

- Admin action buttons not wired to tRPC mutations (known todo)
- Analyse section pages have too much text, need visual-first rebuild (depends on Track 1)
- Cover overlay shows 60 bronnen instead of 427 (evidenceItemsForDiscover is filtered)
- Responsive behavior untested across all pages
- Offerte brochure unchanged but shared components were refactored — verify no regression
- Session tracking untested end-to-end

**Suggested skill:** `/qa` for systematic QA with fix loop, or `/qa-only` for report first

## Track 3: Integration & Polish

Things that need connecting:

- Apollo API key expired (401) — blocks company/contact search
- Admin prospect detail action buttons → tRPC mutations
- Cal.com embed on contact page — untested
- Email templates still reference old copy (may need update for new analyse URL)
- The `evidenceItemsForDiscover` count is wrong — uses filtered list, should use total

**Suggested skill:** `/gsd:progress` to assess, then `/gsd:plan-phase` per item

</remaining_work>

<decisions_made>

- Analyse page is visual-first: charts/scores as primary content, text behind "Lees meer" modals (feedback memory saved)
- No rust/brown colors — only navy + gold + white + grey palette
- BrandChrome shows `K ✕ {companyName}` on all pages (both analyse and offerte-capable)
- Cover video: "OUTSMART YOUR INDUSTRY" with dynamic stats overlay
- Stats overlay: labels left, gold numbers right-aligned, positioned right of video header
- Kansen page: future milestone will add click-through to klarifai.nl projects and custom solutions
- Section layout follows content: quote layout for NEWS citations, pillars for 3-part structures, split as default
- No fake data: all visual elements must be derivable from real scraped evidence
  </decisions_made>

<blockers>
- Apollo API key expired (401) — blocks company/contact search enrichment
- Master prompt generates text-only output — blocks visual-first section pages
- Cover overlay evidence count uses filtered list (60) instead of total (427)
</blockers>

<context>
Romano's frustration this session: the brochure had too much text, fake percentages, and poorly positioned elements. The core insight is that a cold outreach brochure must be VISUAL first — nobody reads walls of text from an unknown company. The AI-generated narrative is valuable but belongs behind a click, not as the primary content.

The next session should NOT jump straight into coding. First step: figure out which skills/tools to use for a proper QA sweep and plan the prompt engineering work. Use `/gsd:progress` to assess state, then plan methodically.

Key feedback memories saved:

- `feedback_brochure_visual_first.md` — visual-first, text behind clicks
- `feedback_visual_first_process.md` — always mockup before coding (from previous session)
- `project_kansen_page_vision.md` — kansen should offer custom solutions, not just existing use cases
  </context>

<next_action>

1. Start with `/gsd:progress` to see full project state
2. Run `/qa-only` on the full app to get a bug report without fixing yet
3. Use `/gsd:plan-phase` for the prompt engineering work (Track 1)
4. Then systematically fix issues from QA report
5. Only then implement visual-first section pages (depends on prompt changes)

Do NOT start coding immediately. Assess first, plan second, execute third.
</next_action>
