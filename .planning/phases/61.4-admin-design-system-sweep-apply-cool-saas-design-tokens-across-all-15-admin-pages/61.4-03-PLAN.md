---
phase: 61.4-admin-design-system-sweep
plan: 03
type: execute
wave: 2
depends_on: ['61.4-01', '61.4-02']
files_modified:
  - app/admin/layout.tsx
  - app/admin/page.tsx
  - app/admin/outreach/page.tsx
  - app/admin/campaigns/new/page.tsx
  - app/admin/campaigns/[id]/page.tsx
  - app/admin/campaigns/page.tsx
  - app/admin/settings/page.tsx
autonomous: true
requirements:
  - SIDEBAR-HOVER
  - PAGE-SWEEP-A
must_haves:
  truths:
    - 'Sidebar nav items show hover tooltip labels on mouse hover'
    - 'Dashboard page uses token-based colors and shared components (PageHeader, SectionHeading)'
    - 'Outreach page uses token-based colors and shared components'
    - 'Campaigns pages use token-based colors and shared components'
    - 'Settings page uses token-based colors and shared components'
  artifacts:
    - path: 'app/admin/layout.tsx'
      provides: 'Sidebar with hover-expand tooltip labels'
      contains: 'group-hover:opacity-100'
    - path: 'app/admin/page.tsx'
      provides: 'Token-migrated dashboard'
    - path: 'app/admin/outreach/page.tsx'
      provides: 'Token-migrated outreach page'
  key_links:
    - from: 'app/admin/page.tsx'
      to: 'components/ui/section-heading.tsx'
      via: 'import { SectionHeading }'
      pattern: 'SectionHeading'
---

<objective>
Add sidebar hover-expand tooltip labels and sweep the heaviest admin pages (dashboard, outreach, campaigns/*, settings) to replace all hardcoded Tailwind colors with CSS variable tokens and shared components.

Purpose: These 7 files contain ~290 hardcoded color patterns combined — they represent 60% of the total sweep work.
Output: 7 updated admin page files with zero hardcoded color patterns.
</objective>

<execution_context>
@/home/klarifai/.claude/get-shit-done/workflows/execute-plan.md
@/home/klarifai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/61.4-admin-design-system-sweep-apply-cool-saas-design-tokens-across-all-15-admin-pages/61.4-CONTEXT.md
@.planning/phases/61.4-admin-design-system-sweep-apply-cool-saas-design-tokens-across-all-15-admin-pages/61.4-RESEARCH.md
@.planning/phases/61.4-admin-design-system-sweep-apply-cool-saas-design-tokens-across-all-15-admin-pages/61.4-01-SUMMARY.md
@.planning/phases/61.4-admin-design-system-sweep-apply-cool-saas-design-tokens-across-all-15-admin-pages/61.4-02-SUMMARY.md
@DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sidebar hover-expand tooltips + sweep dashboard page</name>
  <files>app/admin/layout.tsx, app/admin/page.tsx</files>
  <read_first>
    - app/admin/layout.tsx (current sidebar RailItem — already 58px rail with icon-only nav, needs hover tooltip)
    - app/admin/page.tsx (current dashboard — 35 hardcoded patterns to replace)
    - components/ui/section-heading.tsx (new shared component from Plan 02 — for dashboard section headings)
    - components/ui/page-header.tsx (new shared component from Plan 02)
    - components/ui/empty-state.tsx (new shared component from Plan 02)
    - app/globals.css (token vocabulary reference)
    - .planning/phases/61.4-admin-design-system-sweep-apply-cool-saas-design-tokens-across-all-15-admin-pages/61.4-RESEARCH.md (Pattern 1: Token Mapping Table — the complete substitution reference)
  </read_first>
  <action>
    **Sidebar hover tooltip in `app/admin/layout.tsx`:**

    In the `RailItem` component, add a tooltip label that appears on hover. After the `<item.icon>` element, add:

    ```tsx
    <span className="pointer-events-none absolute left-full ml-3 hidden whitespace-nowrap rounded-[var(--radius-xs)] bg-[var(--color-ink)] px-2.5 py-1 text-[11px] font-medium text-[var(--color-surface)] opacity-0 transition-opacity group-hover:opacity-100 lg:block">
      {item.label}
    </span>
    ```

    The parent Link already has `group` in its className. If not, add `group` to the Link's cn() call.

    **Dashboard `app/admin/page.tsx` token sweep:**

    Apply the token mapping table from RESEARCH.md systematically:

    - Replace ALL `bg-slate-*` → `bg-[var(--color-surface-2)]` or `bg-[var(--color-surface)]`
    - Replace ALL `text-slate-*` / `text-gray-*` → `text-[var(--color-muted)]` or `text-[var(--color-ink)]`
    - Replace ALL `border-slate-*` → `border-[var(--color-border)]` or `border-[var(--color-border-strong)]`
    - Replace ALL `text-[#040026]` / `text-[#0a0a2e]` → `text-[var(--color-ink)]`
    - Replace ALL `bg-[#FCFCFD]` → `bg-[var(--color-surface)]`
    - Replace ALL `font-black` → `font-bold`
    - Replace ALL `tracking-widest` → `tracking-[0.14em]`
    - Replace ALL `rounded-xl` → `rounded-[var(--radius-md)]`, `rounded-2xl` → `rounded-[var(--radius-lg)]`
    - Remove ALL `ui-tap`, `ui-focus` class references
    - Remove ALL `shadow-inner`, `shadow-xl` decorative shadows
    - Replace `bg-emerald-*`/`bg-blue-*`/`bg-purple-*`/`bg-amber-*` semantic colors with `admin-state-*` classes where used as status indicators, or `var(--color-tag-*-bg/text)` for activity feed type badges
    - Replace inline h1 header pattern with `<PageHeader title="..." />` import
    - Replace inline SectionHeading function (if it exists as a local component) with `<SectionHeading>` import from `components/ui/section-heading`
    - Replace inline empty state blocks with `<EmptyState>` import

    Keep the page's layout and structure identical — only swap colors, typography, and markup for shared components.

  </action>
  <verify>
    <automated>grep -c "bg-slate\|text-slate\|text-gray\|border-slate\|font-black\|ui-tap\|ui-focus\|shadow-inner\|shadow-xl\|text-\[#040026\]\|bg-\[#FCFCFD\]\|tracking-widest" app/admin/page.tsx | grep -q "0" && grep -q "group-hover:opacity-100" app/admin/layout.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `app/admin/layout.tsx` RailItem contains a span with `group-hover:opacity-100` (hover tooltip)
    - `app/admin/layout.tsx` RailItem tooltip uses `bg-[var(--color-ink)]` and `text-[var(--color-surface)]`
    - `app/admin/page.tsx` contains zero instances of: `bg-slate`, `text-slate`, `text-gray`, `border-slate`, `font-black`, `ui-tap`, `ui-focus`, `shadow-inner`, `shadow-xl`, `text-[#040026]`, `bg-[#FCFCFD]`, `tracking-widest`
    - `app/admin/page.tsx` imports `PageHeader` from `@/components/ui/page-header`
    - `app/admin/page.tsx` imports `SectionHeading` from `@/components/ui/section-heading`
  </acceptance_criteria>
  <done>Sidebar has hover tooltip labels; dashboard page is fully token-migrated with zero hardcoded Tailwind color classes</done>
</task>

<task type="auto">
  <name>Task 2: Sweep outreach + campaigns + settings pages</name>
  <files>app/admin/outreach/page.tsx, app/admin/campaigns/new/page.tsx, app/admin/campaigns/[id]/page.tsx, app/admin/campaigns/page.tsx, app/admin/settings/page.tsx</files>
  <read_first>
    - app/admin/outreach/page.tsx (74 hardcoded patterns — heaviest file)
    - app/admin/campaigns/new/page.tsx (76 hardcoded patterns — heaviest file)
    - app/admin/campaigns/[id]/page.tsx (39 hardcoded patterns)
    - app/admin/campaigns/page.tsx (9 hardcoded patterns — lighter)
    - app/admin/settings/page.tsx (37 hardcoded patterns)
    - components/ui/page-header.tsx (shared PageHeader)
    - components/ui/empty-state.tsx (shared EmptyState)
    - components/ui/stat-card.tsx (shared StatCard — for settings stat blocks)
    - app/globals.css (token vocabulary + admin-btn-*, admin-toggle-*, admin-eyebrow, admin-meta-text classes)
    - .planning/phases/61.4-admin-design-system-sweep-apply-cool-saas-design-tokens-across-all-15-admin-pages/61.4-RESEARCH.md (Pattern 1: Token Mapping Table)
  </read_first>
  <action>
    Apply the same systematic token mapping to all 5 files. For each file:

    **Color substitutions (apply to ALL files):**
    - `bg-slate-50` / `bg-slate-100` → `bg-[var(--color-surface-2)]`
    - `bg-white` / `bg-[#FCFCFD]` → `bg-[var(--color-surface)]`
    - `text-slate-300` / `text-slate-400` / `text-slate-500` / `text-gray-400` / `text-gray-500` → `text-[var(--color-muted)]`
    - `text-slate-600` / `text-slate-700` / `text-gray-600` / `text-gray-700` → `text-[var(--color-muted-dark)]`
    - `text-slate-800` / `text-slate-900` / `text-gray-800` / `text-gray-900` / `text-[#040026]` / `text-[#0a0a2e]` → `text-[var(--color-ink)]`
    - `border-slate-100` / `border-slate-200` → `border-[var(--color-border)]`
    - `border-slate-300` → `border-[var(--color-border-strong)]`
    - `bg-emerald-50 text-emerald-500/600` → `admin-state-success` (or `bg-[var(--color-tag-enrich-bg)] text-[var(--color-tag-enrich-text)]`)
    - `bg-blue-50 text-blue-500/600` → `admin-state-info` (or `bg-[var(--color-tag-run-bg)] text-[var(--color-tag-run-text)]`)
    - `bg-purple-50 text-purple-500/600` → `admin-state-accent`
    - `bg-amber-50 text-amber-500/600` → `admin-state-warning`
    - `bg-red-50 text-red-500/600` → `admin-state-danger`

    **Typography substitutions:**
    - `font-black` → `font-bold`
    - `tracking-widest` → `tracking-[0.14em]`
    - `text-[9px] font-bold uppercase` or `text-[10px] font-black uppercase` → use `admin-eyebrow` class
    - `rounded-xl` → `rounded-[var(--radius-md)]`
    - `rounded-2xl` → `rounded-[var(--radius-lg)]`

    **Ghost class removal:**
    - Remove ALL `ui-tap`, `ui-focus` from className strings
    - Remove ALL `shadow-inner`, `shadow-xl` decorative shadows

    **Shared component adoption:**
    - Replace inline h1 headers with `<PageHeader title="..." action={...} />`
    - Replace inline empty state blocks with `<EmptyState title="..." description="..." />`
    - Replace stat blocks in settings with `<StatCard eyebrow="..." value={...} />`
    - Replace inline button styling that duplicates admin-btn-* with the CSS classes: `admin-btn-primary`, `admin-btn-secondary`, `admin-btn-danger`

    **Button class fixes:**
    - Replace any `btn-pill-yellow` in admin pages with `admin-btn-primary` (navy solid, per locked decision)
    - Replace inline gold gradient button styling with `admin-btn-primary` class

    Keep all page layouts and functionality identical — this is a styling-only migration.

  </action>
  <verify>
    <automated>for f in app/admin/outreach/page.tsx app/admin/campaigns/new/page.tsx "app/admin/campaigns/[id]/page.tsx" app/admin/campaigns/page.tsx app/admin/settings/page.tsx; do count=$(grep -c "bg-slate\|text-slate\|text-gray\|border-slate\|font-black\|ui-tap\|ui-focus\|shadow-inner\|shadow-xl\|tracking-widest" "$f" 2>/dev/null || echo 0); if [ "$count" -gt 0 ]; then echo "FAIL: $f has $count hardcoded patterns"; exit 1; fi; done && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `app/admin/outreach/page.tsx` contains zero instances of: `bg-slate`, `text-slate`, `text-gray`, `border-slate`, `font-black`, `ui-tap`, `ui-focus`, `shadow-inner`, `shadow-xl`, `tracking-widest`
    - `app/admin/campaigns/new/page.tsx` contains zero of the above patterns
    - `app/admin/campaigns/[id]/page.tsx` contains zero of the above patterns
    - `app/admin/campaigns/page.tsx` contains zero of the above patterns
    - `app/admin/settings/page.tsx` contains zero of the above patterns
    - No `btn-pill-yellow` in any of these admin files (replaced with `admin-btn-primary`)
    - All files import shared components where applicable (PageHeader, EmptyState, StatCard)
  </acceptance_criteria>
  <done>All 5 heavy admin pages are fully token-migrated with zero hardcoded Tailwind color classes; shared components adopted where pattern repetition existed</done>
</task>

</tasks>

<verification>
- `npm run check` passes
- Combined grep across all 7 files returns zero matches for hardcoded patterns:
  `grep -rn "bg-slate\|text-slate\|text-gray\|border-slate\|font-black\|ui-tap\|ui-focus\|shadow-inner\|shadow-xl\|tracking-widest\|text-\[#040026\]\|bg-\[#FCFCFD\]" app/admin/page.tsx app/admin/layout.tsx app/admin/outreach/page.tsx app/admin/campaigns/ app/admin/settings/page.tsx`
</verification>

<success_criteria>

- Sidebar shows hover tooltip labels on all nav items
- Dashboard, outreach, campaigns/\*, settings pages have zero hardcoded Tailwind color classes
- Shared components (PageHeader, SectionHeading, EmptyState, StatCard) adopted where applicable
- All admin-btn-primary usage renders navy solid (not gold gradient)
- `npm run check` green
  </success_criteria>

<output>
After completion, create `.planning/phases/61.4-admin-design-system-sweep-apply-cool-saas-design-tokens-across-all-15-admin-pages/61.4-03-SUMMARY.md`
</output>
