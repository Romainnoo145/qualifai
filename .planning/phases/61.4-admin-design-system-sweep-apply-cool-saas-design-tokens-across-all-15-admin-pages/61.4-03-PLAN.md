---
phase: 61.4-admin-design-system-sweep
plan: 03
type: execute
wave: 2
depends_on:
  - 61.4-01
  - 61.4-02
files_modified:
  - components/features/offerte/brochure-cover.tsx
autonomous: false
requirements:
  - CTA-FIX
  - VISUAL-VERIFY

must_haves:
  truths:
    - 'Offerte brochure cover text is fully visible without truncation'
    - "Offerte brochure BrandChrome shows 'K x {companyName}' on all pages"
    - 'All admin navigation targets work when clicked'
    - 'Analyse brochure section pages show visual elements or graceful text fallback'
  artifacts:
    - path: 'components/features/offerte/brochure-cover.tsx'
      provides: 'BrandChrome with companyName on all pages, fixed cover text'
      contains: 'companyName'
  key_links:
    - from: 'components/features/offerte/brochure-cover.tsx'
      to: 'components/shared/brochure-chrome.tsx'
      via: 'BrandChrome receives companyName prop'
      pattern: 'BrandChrome.*companyName'
---

<objective>
Fix offerte brochure visual issues (truncated cover text, missing company name in chrome) and run visual verification across all three tracks.

Purpose: Fix 2 QA issues (ISSUE-005, ISSUE-012) and verify all fixes from Plans 01 and 02 work in-browser.
Output: Clean offerte brochure, verified admin navigation, verified brochure rendering.
</objective>

<execution_context>
@/home/klarifai/.claude/get-shit-done/workflows/execute-plan.md
@/home/klarifai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.gstack/qa-reports/qa-report-localhost-9200-2026-04-20.md

<interfaces>
<!-- From components/shared/brochure-chrome.tsx -->
<!-- BrandChrome accepts optional companyName prop -->
<!-- When companyName is provided, renders "K x {companyName}" in top-left -->
<!-- When omitted, renders only K logo -->

<!-- From components/features/offerte/brochure-cover.tsx -->
<!-- BrochureCover receives prospect.companyName from page.tsx -->
<!-- BrandChrome is called without companyName on ALL 7 pages (lines 132, 315, 526, 769, 1148, 1569, 2056) -->
<!-- Cover page (page 0) is a video — text truncation may be in video overlay or in page 1 hero text -->
<!-- Uitdaging page (~line 365-380) has hardcoded hero text with font-size: clamp(56px, 6.5vw, 96px) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix offerte BrandChrome and cover text truncation</name>
  <files>components/features/offerte/brochure-cover.tsx</files>
  <read_first>
    - components/features/offerte/brochure-cover.tsx (full file — find all BrandChrome calls, understand cover/Uitdaging page text rendering)
    - components/shared/brochure-chrome.tsx (understand BrandChrome companyName prop API)
    - app/offerte/[slug]/page.tsx (confirm prospect.companyName is passed to BrochureCover)
  </read_first>
  <action>
**Fix 1: Pass companyName to all BrandChrome calls (ISSUE-012)**

The BrochureCover component receives `prospect.companyName` via props but never passes it to BrandChrome. There are 7 BrandChrome calls (lines ~132, 315, 526, 769, 1148, 1569, 2056) — ALL without companyName.

For each call, change:

```tsx
<BrandChrome />
```

to:

```tsx
<BrandChrome companyName={prospect.companyName} />
```

The `prospect` variable is available in the component scope (passed as prop to BrochureCover and threaded to each sub-component like Uitdaging, Aanpak, etc.). Check each sub-component to confirm `prospect` is in scope — it is passed as a prop to each one.

**Fix 2: Fix cover text truncation (ISSUE-005)**

The QA report says "AI SYSTEMEN AUTOMAT" is truncated on the cover. Since the cover (page 0) is a video with no text overlay in code, the truncation is likely on page 1 (Uitdaging) or an intro/tagline rendered somewhere.

Investigate the text rendering near the cover. Check:

1. If there's an overlay text on the video cover that uses `overflow: hidden` or constrained dimensions
2. If the Uitdaging page hero text (~line 365-380) has a `maxWidth` that clips long text
3. If the `quote.onderwerp` field is rendered somewhere and gets truncated

The fix depends on what's found:

- If text overflows a container: add `overflow: visible` or remove `maxWidth` constraint, or reduce font-size with clamp for long text
- If text is in the video: this is a video asset issue — add a text overlay on top of the video that renders `quote.onderwerp ?? prospect.companyName` with proper sizing
- If text is in a fixed-height container: increase height or use `min-height` instead of `height`

The cover page's hero statement should use responsive text sizing that doesn't clip. If the hero h1 uses a very large `clamp(56px, 6.5vw, 96px)`, ensure the container has enough vertical space and the text wraps instead of overflowing.

Key: find the element that renders "AI SYSTEMEN AUTOMAT..." (this is likely the `onderwerp` field from the quote data for Marfa), ensure it renders fully visible. If it's a long string like "AI Systemen & Automatisering", the container or font size needs to accommodate it.
</action>
<verify>
<automated>cd /home/klarifai/Documents/klarifai/projects/qualifai && grep -c "BrandChrome companyName\|companyName={prospect" components/features/offerte/brochure-cover.tsx && grep -c "<BrandChrome />" components/features/offerte/brochure-cover.tsx && npm run check</automated>
</verify>
<acceptance_criteria> - `grep -c "<BrandChrome />" components/features/offerte/brochure-cover.tsx` returns 0 (no BrandChrome calls without companyName) - `grep -c "companyName" components/features/offerte/brochure-cover.tsx` returns 7+ (one per page) - Text container for cover/intro does not use `overflow: hidden` that clips content, OR font size is adjusted for long text - `npm run check` exits 0
</acceptance_criteria>
<done>All BrandChrome calls pass companyName. Cover text renders fully without truncation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Skill-driven visual verification of all 3 tracks</name>
  <files>N/A — verification only</files>
  <action>
Run systematic verification using skills, then present results to user for approval.

**Step 1: Run `/qa` on the full app**
Use the `/qa` skill (NOT `/qa-only` — we want the test-fix-verify loop) targeting http://localhost:9200. This will:

- Re-test all 13 original QA issues
- Fix any regressions found during testing
- Produce before/after health score comparison against the baseline in `.gstack/qa-reports/baseline.json`

**Step 2: Run `/design-review` on the brochure surfaces**
Use the `/design-review` skill to audit visual consistency on:

- `/analyse/mujjo` — check visual elements, citations hidden, brand consistency
- `/offerte/marfa` — check company name in chrome, cover text not truncated

**Step 3: Present results to user**
Show:

- QA health score delta (before: 68/100, after: ?)
- Issues resolved vs remaining
- Design review findings
- Screenshot evidence for each resolved issue

Wait for human approval before marking phase complete.
</action>
<what-built>
Plan 01: Wired Start voorstel button, 4 dossier sidebar links, dashboard Start Outreach button.
Plan 02: Extended NarrativeSection type with visual data, updated master prompt, visual-first brochure rendering, fixed evidence count, hidden citations.
Plan 03 Task 1: Fixed offerte BrandChrome companyName, fixed cover text truncation.
</what-built>
<how-to-verify>
**Automated via skills:** - `/qa` — full regression test against baseline (health score comparison) - `/design-review` — visual audit of brochure surfaces - `/gsd:verify-work` — UAT if user wants formal acceptance testing

    **Manual verification (user confirms):**
    1. Admin navigation: all buttons/links work
    2. Analyse brochure: visual elements or graceful text fallback
    3. Offerte brochure: company name in chrome, no text truncation
    4. Health score improved from 68/100 to 80+

  </how-to-verify>
  <verify>Human approval after reviewing /qa health score delta and /design-review findings</verify>
  <done>All 3 tracks verified via skills. QA health score improved. Design review passed.</done>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
1. `npm run check` passes
2. All BrandChrome calls include companyName
3. Human verifies all navigation targets work
4. Human verifies offerte chrome shows company name
5. Human verifies analyse section pages don't show raw citations
</verification>

<success_criteria>

- ISSUE-005 resolved: Offerte cover text fully visible
- ISSUE-012 resolved: Offerte chrome shows "K x {companyName}"
- All Track 1 fixes verified in-browser (ISSUE-001, ISSUE-002, ISSUE-006)
- Track 2 backward compatibility confirmed (existing analyses still render)
- Track 3 visual polish confirmed
- Health score should improve from 68/100 to 80+
  </success_criteria>

<output>
After completion, create `.planning/phases/61.4-admin-design-system-sweep-apply-cool-saas-design-tokens-across-all-15-admin-pages/61.4-03-SUMMARY.md`
</output>
