---
phase: 61.4-admin-design-system-sweep
plan: 02
subsystem: analyse-brochure
tags: [visual-data, brochure, ai-prompt, types]
dependency_graph:
  requires: []
  provides: [visual-section-types, visual-brochure-rendering]
  affects: [master-prompt, brochure-renderer, analyse-page]
tech_stack:
  added: []
  patterns: [visual-type-discriminated-union, collapsible-details-pattern]
key_files:
  created: []
  modified:
    - lib/analysis/types.ts
    - lib/analysis/master-prompt.ts
    - lib/analysis/master-analyzer.ts
    - components/features/analyse/analyse-brochure.tsx
    - app/analyse/[slug]/page.tsx
decisions:
  - Visual fields are optional on NarrativeSection for backward compat with existing analyses
  - visualData validation passes through when type field matches visualType (permissive)
  - SectionVisualLayout is primary layout choice when visualData present, falls back to text layouts
metrics:
  duration: 280s
  completed: 2026-04-20
---

# Phase 61.4 Plan 02: Visual-First Brochure Rendering Summary

Extended AI master prompt to generate structured visual data per section, fixed evidence count on cover overlay, and updated brochure renderer to display visual elements instead of text walls.

## One-liner

NarrativeSection extended with punchline + visualType + visualData; brochure renders stats/quotes/comparisons/signals as primary content, citations collapsible.

## Tasks Completed

| Task | Name                                                                | Commit  | Key Changes                                                                             |
| ---- | ------------------------------------------------------------------- | ------- | --------------------------------------------------------------------------------------- |
| 1    | Extend NarrativeSection type and update master prompt               | 48cdc53 | Added VisualType/VisualData types, updated both prompt builders, updated validator      |
| 2    | Update brochure renderer for visual elements and fix evidence count | 675898d | Fixed bronnen count, collapsible citations, SectionVisual component with 4 visual types |

## Issues Resolved

- **ISSUE-003**: Section pages now render visual elements (stats/quote/comparison/signals) as primary content when AI provides structured visualData
- **ISSUE-004**: Cover Bronnen count uses `rawEvidenceItems.length` (total evidence) instead of filtered count
- **ISSUE-011**: Raw pipe-separated citations hidden by default in collapsible `<details>` element

## Deviations from Plan

None - plan executed exactly as written.

## Verification

- `npx eslint` passes on all modified files
- `npx tsc --noEmit` produces no errors in modified files
- Backward compatible: existing analyses without visual fields render unchanged (text-based layouts)

## Self-Check: PASSED
