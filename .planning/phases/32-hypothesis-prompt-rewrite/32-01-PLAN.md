---
phase: 32-hypothesis-prompt-rewrite
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/workflow-engine.test.ts
autonomous: true
requirements:
  - ANLYS-01
  - ANLYS-02
  - ANLYS-03
  - ANLYS-04
  - ANLYS-05
  - ANLYS-06
  - ANLYS-07

must_haves:
  truths:
    - "A describe('generateHypothesisDraftsAI') block exists in the test file with Gemini mock"
    - 'Each ANLYS-01 through ANLYS-07 requirement has at least one failing test assertion'
    - 'Running npx vitest run lib/workflow-engine.test.ts shows RED failures for the new tests (existing tests still pass)'
  artifacts:
    - path: 'lib/workflow-engine.test.ts'
      provides: 'Gemini mock + 7 ANLYS test cases'
      contains: 'describe.*generateHypothesisDraftsAI'
  key_links:
    - from: 'lib/workflow-engine.test.ts'
      to: 'lib/workflow-engine.ts'
      via: 'import generateHypothesisDraftsAI'
      pattern: 'import.*generateHypothesisDraftsAI.*from.*workflow-engine'
---

<objective>
Create failing test scaffold for all 7 ANLYS requirements before implementation begins.

Purpose: TDD RED phase — define expected behavior via tests so the prompt rewrite implementation (Plan 32-02) has clear verification targets. Tests cover prompt text content assertions (ANLYS-01, 02, 03, 05, 07), post-parse quote validation (ANLYS-04), and dynamic count behavior (ANLYS-06).

Output: Extended workflow-engine.test.ts with Gemini mock and 7 failing test blocks.
</objective>

<execution_context>
@/home/klarifai/.claude/get-shit-done/workflows/execute-plan.md
@/home/klarifai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-hypothesis-prompt-rewrite/32-RESEARCH.md
@lib/workflow-engine.ts (lines 577-802 — AIEvidenceInput, AIProspectContext, AIHypothesisItem, generateHypothesisDraftsAI)
@lib/workflow-engine.test.ts (existing test structure, mocks, imports)
</context>

<feature>
  <name>generateHypothesisDraftsAI test scaffold</name>
  <files>lib/workflow-engine.test.ts</files>
  <behavior>
    Tests verify the prompt text and output behavior of generateHypothesisDraftsAI:

    ANLYS-01: The prompt string sent to Gemini contains source-tier prioritization instruction mentioning REVIEWS, CAREERS, LINKEDIN as higher priority than WEBSITE.
    Input: standard evidence set → Expected: prompt contains tier ordering instruction

    ANLYS-02: The prompt string contains diagnostic label definitions per source type (REVIEWS = customer pain, CAREERS = operational gaps, WEBSITE = marketing context).
    Input: standard evidence set → Expected: prompt contains "SOURCE TYPE GUIDE" or similar with per-type diagnostic meanings

    ANLYS-03: The prompt string contains an anti-parroting constraint mentioning website/marketing copy.
    Input: standard evidence set → Expected: prompt contains "Do NOT derive hypotheses from" or equivalent anti-parroting text

    ANLYS-04: Post-parse logic warns when a hypothesis problemStatement lacks a quoted snippet.
    Input: mock Gemini returns hypothesis without quotes → Expected: console.warn fires
    Input: mock Gemini returns hypothesis with "quoted text" → Expected: no warning

    ANLYS-05: The prompt string contains a signal summary block with correct tier counts.
    Input: evidence with 2 REVIEWS + 1 CAREERS + 1 WEBSITE → Expected: prompt contains "Diagnostic tier.*3 items" and "Marketing context.*1 item"

    ANLYS-06: Output length varies based on confirmedPainTags argument.
    Input: confirmedPainTags=['planning'] (1 tag) + mock returns 1 hypothesis → Expected: result.length === 1
    Input: confirmedPainTags=['planning','handoff','billing'] (3 tags) + mock returns 3 hypotheses → Expected: result.length === 3
    Input: confirmedPainTags=[] (0 tags, edge case) + mock returns 1 hypothesis → Expected: result.length === 1 (defaults to 1)

    ANLYS-07: The prompt string contains confidence calibration table mapping REVIEWS to 0.80-0.95, CAREERS/LINKEDIN to 0.70-0.80, WEBSITE to 0.60-0.65.
    Input: standard evidence set → Expected: prompt contains all three tier ranges AND "Do NOT score website-derived hypotheses above 0.65"

  </behavior>
  <implementation>
    Add a new `describe('generateHypothesisDraftsAI')` block at the end of the existing test file.

    The Gemini mock strategy:
    1. Add `vi.mock('@google/generative-ai', ...)` BEFORE the existing imports (top of file, after existing mocks)
    2. Create a module-level `mockGenerateContent` vi.fn() that the mock class references
    3. Each test configures `mockGenerateContent.mockResolvedValueOnce(...)` with appropriate JSON response
    4. To capture the prompt text, the mock stores the prompt argument: `mockGenerateContent.mockImplementation((prompt) => { lastPrompt = prompt; return ... })`

    For prompt text assertions (ANLYS-01, 02, 03, 05, 07):
    - Call `generateHypothesisDraftsAI(evidence, context, confirmedPainTags)` with a standard evidence set
    - The mock captures the prompt string passed to `generateContent`
    - Assert the prompt string contains expected text patterns

    For ANLYS-04 (quote validation):
    - Mock Gemini to return a hypothesis WITHOUT quotes in problemStatement
    - Spy on `console.warn` and assert it fires with expected message
    - Second test: mock returns hypothesis WITH quotes, assert no warning

    For ANLYS-06 (dynamic count):
    - Call with `confirmedPainTags` of length 1 → mock returns 1 item → assert output length 1
    - Call with `confirmedPainTags` of length 3 → mock returns 3 items → assert output length 3
    - Call with empty `confirmedPainTags` → mock returns 1 item → assert output length 1

    The mock must return valid HypothesisDraft-compatible JSON including all required fields (title, problemStatement, assumptions, validationQuestions, workflowTag, confidenceScore, evidenceRefs).

    Import `generateHypothesisDraftsAI` — it is currently NOT imported in the test file. Add it to the existing import block from '@/lib/workflow-engine'.

    All new tests will FAIL because the current implementation does not have:
    - Source-tier instructions in the prompt (ANLYS-01, 02)
    - Anti-parroting constraint (ANLYS-03)
    - Quote validation warning (ANLYS-04)
    - Signal summary block (ANLYS-05)
    - confirmedPainTags parameter (ANLYS-06)
    - Source-calibrated confidence table (ANLYS-07)

  </implementation>
</feature>

<verification>
```bash
# RED phase: new tests should FAIL, existing tests should PASS
npx vitest run lib/workflow-engine.test.ts 2>&1 | tail -30
# Expected: existing tests pass, new ANLYS tests fail
# TypeCheck must still pass:
npx tsc --noEmit 2>&1 | tail -10
```
</verification>

<success_criteria>

- Gemini mock (`vi.mock('@google/generative-ai', ...)`) is set up with capturable prompt
- `generateHypothesisDraftsAI` is imported in the test file
- Each of the 7 ANLYS requirements has at least one test assertion
- Running `npx vitest run lib/workflow-engine.test.ts` shows the new tests as FAILING (RED)
- Existing tests in the file continue to PASS
- TypeScript compiles without errors (the 3rd parameter is optional with default `[]`)
  </success_criteria>

<output>
After completion, create `.planning/phases/32-hypothesis-prompt-rewrite/32-01-SUMMARY.md`
</output>
